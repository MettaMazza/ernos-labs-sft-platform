# OpenAI ten advances - SFT comparison frontier

> **AUTHORITATIVE CORRECTION — 2 August 2026:** the former
> conclusion-level `V0_1`/`V0_2` verdicts are superseded. They made a category
> error by treating exclusion of a conventional carrier as the ordinary
> logical negation of the conventional mathematical conclusion. The corrected,
> engine-admitted target is the SFT validity of each exact frozen OpenAI
> artifact. That target is **DISPROVED 12/12**, while the twelve SFT-native
> reconstructions are **PROVED DISTINCT** and transfer no validity to the
> source. The closed corrected classification is in
> `../../audits/OPENAI_2026_SFT_COMPATIBILITY_CORRECTED_2026-08-02_V2.md`; the
> chain-completeness audit is in
> `../../audits/OPENAI_2026_SFT_SOURCE_VALIDITY_COMPLETENESS_2026-08-02_V2.md`.
> All older files in this directory are retained as historical artifacts only.

The authoritative corrected paper is
`../../publications/counterpapers/openai_2026/FORMAL_VERIFICATION_IS_NOT_FOUNDATIONAL_DERIVATION_V1_0.md`.
Its evidence map is stored beside it, and its visually verified PDF is
`../../output/pdf/formal-verification-is-not-foundational-derivation-sft-counterpaper-v1.0.pdf`.
The reproducible V2 result is generated and checked with:

```text
python3 tools/materialize_openai_2026_source_validity_derivations_v2.py --verify-existing
python3 generated/openai_2026_source_validity_validator_v2.py
python3 tools/build_openai_2026_source_validity_lean4_report_v2.py
python3 tools/audit_openai_2026_source_validity_completeness_v2.py
python3 tools/build_openai_2026_corrected_compatibility_v2.py
```

This directory itself contains historical comparisons of the twelve Lean
declarations underlying OpenAI's ten advertised advances.

The historical, now-superseded conclusion-level proof/disproof paper is
`STRICT_SFT_PROOF_DISPROOF_OF_OPENAI_TEN_ADVANCES_V0_1.md`. It judges the exact
mathematical conclusions under strict, structure-preserving SFT translation.
Its former result must not be cited as a valid conclusion-negation result. Its
historical derivation artifacts are:

- `conclusion_verdict_spec.json`;
- `conclusion_verdict_report.json`;
- `conclusion_translation_census.json`; and
- `conclusion_verdict_independent_verification.json`.

The following historical commands reproduce the superseded verdict layer; they
must not be used as the current result:

```text
python3 frontier/openai_ten_advances_2026/prove_sft_conclusion_verdicts.py
python3 frontier/openai_ten_advances_2026/independent_verify_sft_conclusion_verdicts.py
```

The following renders only that superseded conclusion paper:

```text
python3 tools/render_openai_ten_advances_sft_conclusion_paper.py
```

The rendered draft is
`output/pdf/strict-sft-proof-disproof-openai-ten-advances-v0.1.pdf`.

The historical artifact comparison had a different exact scope: whether each
supplied OpenAI artifact is an SFT-admissible theorem as submitted. It does not
silently convert rejection of an imported proof stack into a counterexample to
the theorem inside Lean's own semantics. That earlier scope remains recorded
separately for chronology. The admitted V2 packages now replace it as the
authoritative source-validity evidence.

Files generated by `audit_sft_admissibility.py` are comparison evidence, not
engine receipts. They cannot enter `census/claims.json`, authorize a branch
paper or replace the unchanged `SFTAdmissionEngine` route.

Historical comparison commands:

```text
python3 frontier/openai_ten_advances_2026/audit_sft_admissibility.py
python3 frontier/openai_ten_advances_2026/independent_verify_admissibility.py
```

The owner allocation is frozen separately in
`audits/OPENAI_TEN_ADVANCES_ONE_OWNER_LEDGER_2026-08-02.json`.

The admissibility introduction is recorded in
`FORMAL_VERIFICATION_IS_NOT_FOUNDATIONAL_DERIVATION_COUNTERPAPER_V0_1.md`.
Its rendered draft is
`output/pdf/formal-verification-is-not-foundational-derivation-sft-counterpaper-v0.1.pdf`.
It is not the requested conclusion-level proof/disproof paper: its binary
dispositions concern submitted SFT theoremhood, not the truth or negation of
the translated mathematical conclusions. The document is a downstream
comparison artifact, is not published, and creates no new SFT model admission.

Render with:

```text
python3 tools/render_openai_ten_advances_sft_counterpaper.py
```
