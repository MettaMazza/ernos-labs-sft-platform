# Foundation frontier

The foundation branch is active and its paper gate is closed. The following
claims are model-admitted and independently replicated:

- `SFT-ROOT-THERE-IS-NO-NOTHING` — premise-free operational root;
- `SFT-FOUNDATION-ONE-001` — structural One;
- `SFT-FOUNDATION-COUNT-001` — exact positive finite count trace;
- `SFT-FOUNDATION-PART-001` — exact held/whole part coordinate; and
- `SFT-FOUNDATION-FOLD-001` — minimal structural Fold.

The next dependency is `SFT-FOUNDATION-PART-EQUIVALENCE-001`. It must determine,
without importing multiplication or fraction reduction, when coordinates from
different lawful partitions identify the same held portion through a completely
generated common refinement.

After that result seals, the remaining registered work is:

1. finite Fold composition and exact generated word support;
2. the complete declared foundational form grammar;
3. depth-independent assembled-form minimality and named-shape enforcement; and
4. the one-way boundary between exact derivation and conventional measured data.

No foundation paper is ready while any of these obligations remains without a
model-admitted receipt. The inventory in
`publications/inventories/foundation.json` remains deliberately unfrozen until
its current-knowledge scope has been audited against the completed claim chain.
