# V3 protein-folding computational proof workspace

Status: `exact_3347070_candidate_parent_verified__generalised_partial_recurrence_closed_three_of_three__structural_improvement_one_of_three__full_v3_capability_halt`.

This directory is the clean working area for the Smithian Fold Theory V3
protein-folding computational proof and application rebuild. Work here may
validate or falsify admitted laws, but it may not select, rewrite or silently
strengthen them.

## Headline programme objective

Achieve **AlphaFold generalised blind parity** through a preregistered,
target-inaccessible campaign over genuinely unrelated sequences, fold families,
lengths and declared conditions.

- **Generalised** means performance across independent sequences and structural
  families rather than repeated coordinate deposits of the same sequence.
- **Blind** means that target structures, target-derived features and evaluation
  scores remain inaccessible until the V3 prediction artefacts are sealed.
- **Parity** means matching or exceeding the frozen AlphaFold comparator on the
  same registered targets under the registered aggregate and per-target metric
  rules, with complete coverage and every outcome retained.

The comparator implementation, databases, settings, hardware boundary, target
set, metrics, aggregation rules and victory criterion must be frozen before the
holdout campaign begins.

## Current machine gate

The condition-to-state dependency has been resolved without inventing a direct
pH selector: the condition remains held, all lawful protonation and solvent
states remain plural, and selection belongs to the joint physical order. The
current target-free AGA execution boundary now contains:

- all 343 plural full-atom protonation carriers and their exact electron
  boundaries;
- 4,752 complete one-water states per carrier;
- an exact standard-carrier joint compiler with 572 electron cells, 126 held
  electrons, 33 positive sources and 163,306 distinct electron-pair responses;
- response-preserving spin marginalisation; and
- exact rational and partition-preserving algebraic frontier solvers that agree
  with exhaustive enumeration on their complete registered controls.

The complete fixed-geometry execution has been partitioned into 118 lawful
exchange-sector vectors and 350 exact first-level shards. Two registered deep
shards on the real 572-cell carrier are complete and independently verified.
The six-candidate first shard is nested inside the 354-candidate second shard,
so their distinct union is 354 candidates rather than 360. The second shard
accounts for 32 completed leaves, 322 certified prunes and 13 retained frontier
states. The
earlier unregistered mechanics probe and three controlled implementation
performance aborts remain preserved and were not promoted to scientific results.

A third registered shard fixes the same 506-cell zero prefix and the next
protein cell to one spin pair. It is disjoint from the 354-candidate zero child
and closes 10,266 candidates: 154 completed leaves, 10,112 certified prunes and
46 retained frontier states. Together the two disjoint children reconstruct one
complete 10,620-candidate parent prefix exactly.

The registered fine-grained continuation now closes one exact
3,347,070-candidate parent prefix across 789 disjoint shards. Its 40,899
completed leaves, 3,306,171 certified-pruned candidates and 135 retained
frontier states reconstruct independently with zero failures. The remaining
extension to a 42,173,082-candidate parent is registered before execution as
11,012 disjoint shards covering 38,826,012 new candidates. The remaining
572-cell exchange-sector surface and the complete torsion, protonation and
solvent recurrence remain open. A new target-free arbitrary-sequence
joint-response compiler now composes the complete held torsion,
protonation-state, water-placement, protein-electron and solvent-electron
coordinates into the exact fixed-geometry relation. Its AGA control
reconstructs the prior 572-cell response surface and its non-AGA glycine
control compiles 285 cells and 40,470 distinct pair responses; both controls
reconstruct independently with zero failures. This completes arbitrary-
sequence fixed-geometry compilation, not full state selection or recurrence.
Registered adverse controls also reject both a mismatched held condition and
a solvent nucleus placed on protein electron support.
A registered real non-AGA compiler-to-solver control then exhausts or
certifies all six completions of one held G sector prefix: four completed
leaves, two certified-pruned candidates and two retained frontier states. An
implementation-distinct exhaustive enumeration reconstructs the same exact
two-state frontier. The initial V1 audit-writer failure and its unpromoted
archive remain preserved; the corrected V2 record changes no scientific
grammar or solver rule.
A target-free whole-sector seeding pass now supplies 22 independently
reconstructed, exact feasible incumbents from 100 deterministic routes across
the complete 566-cell protein `q58` and six-cell solvent `q5` sector. These
states span the attraction–repulsion trade-off and are lawful bounds, not a
claim that the frontier is complete. Three registered scalability controls
then tested the existing rectangular bounds against the whole sector. The
depth-12 prefix census visited 8,191 nodes and pruned none of its 4,096 held
surfaces; a degree-strengthened depth-8 census likewise pruned none of 256;
and a complete 59-stratum consensus-count census pruned no stratum. All three
adverse results were independently reexecuted and remain preserved. They show
that full-sector completion requires a coupled attraction–repulsion bound,
not further repetition of the same independent extrema relaxation.
A first exact coupled scalar envelope then excluded none of the 22 incumbent
bands, although it independently certifies the highest-attraction incumbent as
the exact linear optimum. A new 1,143-node spatial hierarchy reconstructs all
572 leaves and lower-bounds every pair response, but its standalone exact
repulsion floor is `97.42645...`, weaker than the prior degree floor
`147.61975...` and below the lowest incumbent response `378.84495...`. The
original generated “improves bound” label is preserved and explicitly
corrected by a reconciliation record. Its quota-preserving coupled dynamic
programme was then executed and independently reexecuted; it excluded none of
the 22 incumbent bands. That fourth adverse bound result is preserved. A
stronger complete-sector cardinality-RLT relaxation, including the full
McCormick hull, raises the exact certified repulsion floor to
`192.197997235382...`. This strictly improves the prior `147.61975...` floor
but remains below the least feasible incumbent `378.84495...`; its separately
frozen exact verifier reconstructs the dual with zero failures. The result is
therefore a positive bound improvement, not minimum or frontier closure. A
first attraction-conditioned RLT test of the highest band is also exact and
independently verified, but adverse: its `1688.211059043998...` lower bound is
the preceding feasible incumbent, below the `1706.611377155384...` exclusion
ceiling. That record identifies the strict attraction boundary as the next
proof obligation. A symbolic-sign-first top-rank implementation was preserved
after a controlled 4,894-second performance interruption with no outcome. Its
interval-first successor completed in 49.7 seconds and proved that the seeded
rank-20 incumbent was not the true attraction runner-up. A complete
interval-monotone census then exhausted all 583 occupations capable of
exceeding that incumbent's attraction boundary. Exact repulsion evaluation
and independent reconstruction retain 10 states, explicitly dominate 573 and
close the entire high-attraction slice. Eight retained states are new
nondominated incumbents; the lower attraction bands remain open.
A target-free generalised
partial-coordinate sequence executor is now implemented and independently
verified on three real sequences: all three recurrences close, but matched
structural accuracy improves on only one of three controls. This is a
computational generalisation result and an adverse structural-generalisation
result, not the certified full-V3 predictor. The full capability halt and the
sealed AlphaFold parity campaign therefore remain open. No current blind
campaign target or AlphaFold campaign output has been accessed.

The latest complete workspace verification checkpoint runs 847 tests with zero failures
or errors. This verifies the implementation and preserved records; it does not
convert the adverse structural panel into empirical confirmation.

## Authoritative starting records

- Protein-folding claim: `claims/SFT-BIO-PROTEIN-FOLD-001/`
- Model-admitted receipt:
  `receipts/engine/model_admitted/SFT-BIO-PROTEIN-FOLD-001-e9c16744ae02fcda.json`
- Registered empirical protocol:
  `experiments/biology/SFT-EXP-BIO-PROTEIN-FOLD-001-E1/registration.json`
- Biology roadmap: `docs/branch_roadmaps/08-biology.md`
- Application boundary: `applications/frontier/README.md`

## Working boundary

- Build from the current V3 corpus and immutable receipts.
- Keep WHY, DERIVATION and CHECK distinct.
- Keep target structures and scores inaccessible before any empirical
  prediction seal.
- Preserve failures, adverse controls, unavailable evidence and condition
  boundaries.
- Do not import a previous Fold Protein implementation as proof authority.
- Do not edit the frozen engine or protected verification authority.
- Do not report this workspace as a scientific admission or completed
  computational proof until the applicable gates actually pass.

No implementation, result, validation, admission, push or publication is
asserted by creation of this directory.

## Workspace map

- `spec/` — exact application scope, dependency binding, terminology and
  output constitution.
- `provenance/` — V2 artefact classification and source-custody records.
- `protocols/` — development and target-inaccessible empirical protocols.
- `kernel/` — exact finite representations and joint compatibility relations.
- `solver/` — exhaustive and certified structure-preserving solvers.
- `evaluators/` — post-seal structural and ensemble evaluation.
- `tests/` — unit, exhaustive, metamorphic, adverse and replay checks.
- `evidence/development/` — development-observed records only.
- `evidence/holdout/` — sealed holdout registrations and post-seal outcomes.
- `audits/` — completeness, pruning, invariance and reconciliation audits.
- `paper/` — conceptual paper and scientific audit-layer material.

The machine-readable workspace state is recorded in
`workspace_manifest.json`.

The durable restart position is recorded in
`audits/continuation_checkpoint.json`.

The completed exact-representation phase is recorded in
`audits/exact_representation_kernel_v1.json`.

The local backbone geometry-and-chirality support family is complete at its
declared scale-free boundary. Five of seven mandatory whole-chain support
families are now complete, including source-bound peptide geometry, the
complete exact conformation fibre and the all-pairs nonlocal correlation
ledger; the predictor remains gated while two are unbound.

The first no-extra-rule reversible transition probe is complete and adverse: it
retains every predecessor but connects the whole tested conformation fibre into
one nondiscriminating recurrent class. The transition family therefore remains
unbound pending a sequence-generated descent relation.

The frozen dependency audit confirmed that no such discriminator was present
in dependency matrix v1. Maria Smith has now authorised the first controlled
route: a versioned target-free SFT protein interaction and descent derivation.
The original gap and dependency records remain unchanged. Version 2 registers
the extension without modifying protected authority.

The registered eight-axis grammar has been exhausted: all 256 forms have exact
decisions, 255 are rejected and one preservation-complete componentwise form
survives; an implementation-distinct reconstruction passes. Source custody now
covers all twenty standard wwPDB amino-acid components and the complete
protonation-variant companion dictionary. Generic exact partial-order semantics
and all 400 ordered standard-residue peptide interfaces are also complete.

The complete plural protonation surface is also retained: 20 current baselines,
198 parent-bound variants and all 47,524 ordered state interfaces, including
five distinct released-carrier classes. No condition-specific state is selected.

All 20 standard residue codes now have an executable, independently verified
current-baseline heavy-atom constructor. The successive frozen batches close
`A`, `C`, `D`, `E`, `F`, `G`, `H`, `I`, `K`, `L`, `M`, `N`, `P`, `Q`, `R`,
`S`, `T`, `V`, `W` and `Y`. All candidate and decision partitions, source
bindings, independent checks and retained adverse records remain in their
versioned specifications, audits and machine archives.

The first proline ring-closure batch is complete and adverse at the frozen
ten-direction unit-step boundary: all 252 states are empty and all 2,376
accepted partial paths terminate at an empty exact CD-to-CG/N shell
intersection. The zero-survivor archive is preserved. Proline remains open;
this adverse result is not a claim that physical proline rings do not exist.

The first monotone successor has now also been exhausted. Its 26-direction set
strictly contains all ten predecessor directions, yet all 6,408 accepted
partial paths again terminate at empty exact closure intersections. Both
adverse archives remain authoritative. Further resolution expansion is halted
until the exact rational equal-unit ring equation is analysed independently of
any desired outcome.

That analysis is now complete. A parity-and-infinite-descent proof establishes
that no odd number of rational unit vectors in three dimensions can sum to
zero. Proline closure would require five such vectors, so no further rational
direction resolution can repair the equal-unit carrier. The next required
support is a target-free, source-bound unequal bond-magnitude carrier. This is
a representation obstruction, not a claim that physical proline rings are
absent.

The proline bond-length authority was audited before extraction. Its frozen,
target-free registration binds all five ring edges to the captured wwPDB
ideal-reference field surface. The completed extraction retains literal
decimals and exact squared magnitudes while forbidding source coordinate
directions from entering generated fold candidates.

The registered extraction is complete. All five ring-edge magnitudes are
distinct, and the complete seven-bond internal-heavy surface also contains
seven distinct magnitudes. Magnitude-only generation at the exact source grid
produces seven complete nonempty lattice shells containing 3,456 to 24,384
vectors per bond.

The frozen certified solver has now exhausted that surface without sampling or
target access. It returns 49,889,500 exact closed-ring proper-rotation generator
fibres and 4,452,985,105,687,787 retained L-embedding orbits. The complete
11,631,724,277,548,915,428,195,041,280-candidate decision partition closes
exactly. An independent verifier traverses every generator record and 1,409
complete C/O controls with zero failures; a clean rerun reproduces the
1,596,464,016-byte logical archive exactly. The archive is stored in seven
hash-bound Git-compatible parts. Proline is therefore complete at the
registered current-baseline unequal-magnitude embedding boundary, while its
two earlier equal-unit adverse results remain preserved.

The frozen isoleucine successor is also complete. It exhausts 1,718,208 exact
candidates across all 252 backbone states: 542,512 retained S-sign embeddings,
542,624 reflected rejections and 633,072 degenerate rejections. Its 119,520
mirror-closure failures are retained as adverse controls; every valid mirror is
an exact involution. The four-part archive passes full streaming identity and
state reconciliation, 1,651 exact geometry and mirror controls, and a
byte-identical clean rerun.

The leucine successor exhausts a further 1,716,936 labelled embeddings. Its
registered `CD1 ↔ CD2` graph automorphism closes without failures or fixed
points, producing 858,468 exact two-member chemical-topology orbits. The
three-part archive passes full streaming identity, terminal-swap and state
reconciliation, exact geometry controls, and a byte-identical clean rerun.

The later batches close glutamate/glutamine, methionine, lysine, arginine,
phenylalanine and tyrosine at the same declared current-baseline layer.
Histidine is closed by its exact unequal-magnitude five-membered-ring solver,
including an independent attachment reconstruction and an unfiltered ring
rerun. Tryptophan's direct magnitude-only fused-ring contraction remains an
explicit open control; its successor instead closes a complete rigid-relative
Gram constructor under the admitted molecular-geometry boundary, with 48
labelled shapes in two proper-rotation orbits and 114,048 retained embeddings.

The representation is therefore executable for 20/20 residue codes. A
separately frozen, source-bound catalogue now places every complete residue
ideal-reference surface under one translation-free Gram schema: 167 retained
heavy-atom records, exact rank three for all 20 components, exact basis
reconstruction throughout and exact W predecessor projection. An
implementation-distinct reconstruction passes with zero failures.

This catalogue is reference custody, not a universal physical geometry or a
complete conformer surface. The next exact operation is to freeze and verify
the condition-held conformational, scale, uncertainty and interface mapping
between the dimensionless exact carrier and physical-coordinate outputs before
whole-chain prediction composition.

The first conformational substage is complete. A topology-only census gives
all 153 retained component bonds exact decisions and retains 38 side-chain
rotor variables with 72 complete outer-carrier pairs. The polymer template adds
`phi`, `psi` and unspecialised `omega` at every consecutive interface, for
`3(n−1)` further variables. Neither cis/trans identity nor any 24-sector state
is selected. Independent source and graph reconstruction passes with zero
failures.

The exact torsion-word substage is now complete as well. Every sequence has a
deterministic ordered variable surface and the complete lazy count
`24^(sum(side-chain rotors)+3(n−1))`. Alanine, cysteine and `AG` witnesses
contain 1, 24 and 13,824 words respectively; primary and independent complete
stream identities agree. Every 24-sector reversal and every residue
automorphism group passes. Leucine and valine each have two labelled-atom
automorphisms, but all twenty residues have one distinct action on the
registered central-bond variables. Seven adverse controls and 15 focused tests
pass with no failure.

The sector-to-source-bound reference-coordinate renderer is now complete. A
primary exact algebraic implementation and an
independent axis-angle matrix reconstruction agree across all 912 registered
single-rotor state cases. They preserve every source-graph bond distance, both
axis endpoints, every fixed atom and the exact reversal. All 25,416 numerical
display components are enclosed by certified 60-digit rational intervals with
zero failure. The complete A, C and L witnesses contain 1, 24 and 576 distinct
exact coordinate outputs respectively. Nine adverse controls pass.

The plural scale, condition, uncertainty and peptide-interface custody surface
is now complete and independently verified: 17 source records, 60 local
assignments, 1,600 ordered residue-interface assignments, 218 retained
protonation states and 47,524 protonation interfaces close with no imputation,
condition selection or verification failure. The source-bound whole-chain word
renderer is also executable. It emits every retained heavy atom, composes the
registered residue-class backbone and peptide means, and applies all backbone
and side-chain sectors without target access. Six focused renderer tests pass.

The first 31-residue historical reconstruction control exposed a clerical
geometry defect: carbonyl O remained in its local reference frame while the
successor N moved with psi, producing seven atom pairs below `0.5 A`. That
complete defective record remains preserved. The corrected constructor retains
the same CCD C-O length and CA-C-O angle while composing nonterminal O trans to
the successor N in the peptide plane. Its 239-atom, 244-bond control contains
all 28,441 unordered atom pairs, 56,882 endpoint incidences and 6,740,517
shared-atom correlations, with no atom pair below `1 A`. The 25.7 MB canonical
ledger reconstructs independently with zero failure and applies no distance
cut-off, force field, target feature or comparator output.

The corrected control remains adverse at the C-alpha boundary: TM
`0.08579343893330212` and C-alpha RMSD95 `6.25666027980953 A`. The carbonyl-only
correction leaves those metrics unchanged and confirms that corrected V3
coordinate custody does not rescue the excluded V2 target-derived selector.
Both versions remain development controls, never V3 proof premises or holdout
results.

The local campaign evaluator and the strict 100-row AlphaFold parity decision
kernel are now executable and frozen. On the retained 4APD development
control, the independent V3 metric implementation exactly reproduces
`tm_repo = 0.08579343893330212` and C-alpha RMSD95
`6.25666027980953 A`, and additionally records lDDT-C-alpha
`0.5241935483870968`. The decision kernel enforces the five registered length
strata, retains every failed, invalid and unavailable row, and applies the
complete seven-part parity conjunction. `tm_repo` remains a historical
diagnostic; it is not silently substituted for the registered external
full-chain TM-score. Official TM-align is now source-captured, compiled and
frozen as the canonical external full-chain evaluator. The complete current
workspace suite contains 847 passing tests and no failures.

The preregistered predictor execution gate first produced a sealed
`capability_halt` on the corrected 4APD development ledger because the six
mandatory physical accounts were then incomplete. That historical capability
result remains preserved: it emitted no representative, prediction PDB or fold
claim and recorded zero target accesses.

A hash-bound assessment of the then-current Chemistry and Physics claim
packages closed the historical coordinate census. All six required structural
laws are present, and exact nuclear and source-formal-charge partial responses
now vary with torsion, but none supplies a complete protein-specific
coordinate: its result was `0/6` complete families, with no failed transport.
Their exact componentwise composition has 240 dominance edges and three
nondominated states, so it lawfully preserves rather than invents the unresolved
choice. The complete all-pair recurrence has three closed classes; the
cyclic-neighbour control has four. Exact-point exclusion accepts all 24 states
without importing a finite radius. A hash-bound current-authority census
decides 17 candidate height routes and finds zero complete-height survivors.
The current CCD baseline contains
167 retained heavy-atom identities, 164 zero formal-charge inscriptions and
only three positive inscriptions; it is therefore not a complete atom-state
charge field. The polarizability, solvation and state-order sources explicitly
remain finite named-molecule evidence rather than lookup rules for an emitted
protein geometry.

Maria Smith's later authorised joint-microstate extension supersedes that
`0/6` census as the current constructor inventory without deleting its
historical result. One preserving form survives all 256 registered candidates.
It represents condition and proton ownership, elementary charge occurrences,
exclusion and exchange, polarisation through explicit electron redistribution,
explicit solvent, and componentwise electronic order as six separately typed
axes. The fixed-geometry compiler and generic certified solver are complete at
their declared boundary. The active dependency is now full frontier execution
followed by the complete torsion–protonation–solvent recurrence across arbitrary
sequences. The certified full-V3 predictor gate remains halted because that
execution is incomplete, not because the six preserving relation axes remain
absent.

A separate sequence-only development track has lawfully opened three targets
after their predictions were sealed. The fresh 4B19 baseline was adverse. The
post-target hydrophobic-majority alpha diagnosis was then tested on unseen
8HJC and was adverse at the plural-cysteine boundary. A narrowed
hydrophobic-majority rule with at most one cysteine transferred positively to
unseen 1JDM, with median official full-chain TM-score `0.45609` over all 16 NMR
models. These results remain development evidence and do not become a certified
full-V3 solver or primary parity row.

The subsequent target-free partial-coordinate recurrence has also been run on
all three now-open sequences. It closes and independently verifies on 4B19,
8HJC and 1JDM, producing 46 complete structure-to-NMR-model comparisons. Median
TM-score improves relative to the preserved predecessor on 4B19 (`0.23779`
versus `0.17500`) but regresses on 8HJC (`0.139675` versus `0.18242`) and 1JDM
(`0.245185` versus `0.45609`). The resulting panel establishes executable
arbitrary-sequence recurrence at the partial-coordinate boundary while
rejecting the present relation as a structurally general selector. These runs
are post-target development controls, not new blind results.

The AlphaFold 3 source commit and deterministic archive are frozen. Its pinned
local environment is installed; all seven official data tests pass, HMMER is
present and the experimental Apple MPS device is visible. The official
inference suite retains one pass and 16 errors because its example tests request
a CUDA-labelled backend and the restricted model parameters are absent. The
model parameters, registered full databases and authorised full-data compute
remain outstanding. No AlphaFold campaign output or parity result is claimed.

Two target-free, torsion-dependent physical subcoordinates are now executable.
The exact heavy-nuclear inverse-square response gives 24 distinct values on the
registered AGA witness. The retained source-formal-charge response gives 24
distinct values on the registered KK witness without promoting formal charge
to a physical partial charge. Their exact componentwise KK composition has 240
dominance edges and three nondominated states, `21`, `22` and `23`; the nuclear
coordinate uniquely prefers `23` while formal-charge opposition uniquely
prefers `21`, so the tradeoff is retained rather than hidden by a fitted weight.

The electronic and proton-state support has also advanced. A complete 420-row
standard-residue and ordered-dipeptide census preserves electron occurrence
counts from 40 to 206 and the current positive H, K and R source charge
inscriptions. The initial false neutrality assumption remains archived as an
adverse execution. Across all 218 source protonation states, 114 strict named-
proton edges generate all 12,996 donor–acceptor transfer templates with total H
and formal charge conserved; 55 isolated source states remain explicit. The
exclusion audit nevertheless finds no lawful geometry map or scale: electron
occurrence and exchange organisation are complete, but orbital assignment,
condition-specific proton order, hydrogen geometry and full electronic charge
distribution remain open. These results narrow the full-V3 gate but do not yet
authorise a fold prediction or primary AlphaFold campaign.

Maria Smith's authorised target-free extension has now been executed beyond
that earlier census. The expanded audit decides 41 existing routes and six
compositions, with no complete pre-existing physical-height survivor. A newly
registered 256-form joint protein/electron/explicit-solvent grammar has one
preservation-complete relation form. Its exact small boundary contains 12
states, 180 pair-response records, 52 descent edges and two closed recurrent
classes; the refined 56-state boundary contains 764 descent edges and ten
closed recurrent classes. Independent reconstruction, 62,000 dyadic distance
identities, 372,000 response identities and 12 adverse controls all pass.

The complete finite electron-occupancy quotient is executable and independently
reconstructed. On a five-cell witness it retains all 1,024 occupancies across
electron counts zero through ten. On the emitted AGA heavy carrier, exact
coarse and refined electron boundaries contain 377 and 2,430 cells for the 93
heavy-subsystem electrons, with exact binomial cardinalities and append-only
support verified independently.

The authoritative peptide graph also exposed a boundary discrepancy that is
now preserved explicitly: the emitted AGA carrier has 14 heavy nuclei, while
the standard-baseline retained graph has 15 heavy nuclei and 15 hydrogens. The
missing heavy nucleus is terminal OXT. A complete source-bound attachment
grammar now covers OXT and all 15 hydrogens through 16 rooted routes. The
declared coarse and refined direction surfaces contain 10^16 and 26^16 raw
joint candidates; 576 exact symbolic radius identities and an independent raw-
CIF reconstruction pass. This is a complete candidate grammar, not a selected
nuclear geometry or condition-specific protonation state.

The admitted finite reversible-generator/Hamiltonian correspondence is now
realised on the electron lattice without importing a continuum Hamiltonian or
an energy coefficient. The exhaustive four-cell witness has 28 occupancies and
96 reversible, particle- and spin-preserving local actions. At the protein
boundary, exact combinatorial certificates count the complete coarse and
refined action incidences without enumerating every occupancy, and all 1,938
coarse spatial actions embed as two-step refined paths.

Scientific gate v9 records six remaining physical-completion objects:
condition-selected protonation plus collision/stereo survivor filtering; a
full 30-nucleus, 116-electron conditioned boundary; same- and near-support
exchange response; physical energy scaling for the finite action relation; a
condition-forced explicit-solvent boundary; and a protein-scale certified joint
marginal solver. Until these close, no protein fold, primary holdout prediction
or AlphaFold parity result is authorised.

The standard-baseline successor now resolves the retained-nucleus integration
at its stated scope. Exact CCD atom-to-parent displacements are transported
through exact residue frames and registered side-chain rotations, producing a
30-nucleus, 29-bond AGA reference carrier with all 16 attachment magnitudes
preserved, zero exact nuclear collisions and 116 electrons. Its algebraic
nuclear enclosure generates 566 coarse and 3,756 refined electron cells,
exact occupancy cardinalities and exact reversible-action incidence counts.
All 2,976 coarse spatial actions embed through refined midpoints. Both the
full-atom carrier and its electron/action boundary reconstruct independently.
Scientific gate v10 therefore replaces the earlier heavy-only integration
gap with the narrower condition-selection gap: the result is complete for the
standard CCD baseline, while condition-selected protonation variants, finite
physical exchange and energy, explicit solvent and protein-scale joint
marginal closure remain open.
