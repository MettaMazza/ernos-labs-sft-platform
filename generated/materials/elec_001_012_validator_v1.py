#!/usr/bin/env python3
from fractions import Fraction
from hashlib import sha256
from itertools import product
import json,sys
from pathlib import Path
REL={
"SFT-MAT-ELEC-CONDUCTIVITY-RESISTIVITY-001":"reciprocal-conductivity-resistivity-specimen-ledger","SFT-MAT-ELEC-MOBILITY-CONCENTRATION-002":"concentration-mobility-separated-carrier-flow","SFT-MAT-ELEC-HALL-RESPONSE-003":"hall-current-field-density-thickness-orientation-ledger","SFT-MAT-ELEC-DIELECTRIC-LOSS-004":"dielectric-stored-lost-cycle-response","SFT-MAT-ELEC-IONIC-TRANSFERENCE-005":"species-labelled-ionic-transference-partition","SFT-MAT-ELEC-MIXED-TRANSPORT-006":"simultaneous-ionic-electronic-complementary-transport","SFT-MAT-ELEC-FINITE-BARRIER-TUNNELLING-007":"finite-barrier-incident-transmitted-reflected-partition","SFT-MAT-ELEC-BAND-ALIGNMENT-008":"two-level-offset-held-order-interface-alignment","SFT-MAT-ELEC-CARRIER-CONFINEMENT-009":"below-barrier-layer-held-carrier-confinement","SFT-MAT-ELEC-DEFECT-TRAP-STATES-010":"defect-occupied-unoccupied-trap-ledger","SFT-MAT-ELEC-SCREENING-DEPLETION-011":"accumulation-depletion-inversion-screening-partition","SFT-MAT-ELEC-ELECTROCHEMICAL-INSERTION-012":"host-inserted-removed-operando-state-path"}
def canon(v):return "sha256:"+sha256(json.dumps(v,sort_keys=True,separators=(",",":"),ensure_ascii=False).encode()).hexdigest()
def fh(p):return "sha256:"+sha256(p.read_bytes()).hexdigest()
def native(c):
 tests=(Fraction(2*2,4)*Fraction(4,2*2)==1,Fraction(6,2*3)==1,Fraction(6*2,2*2*3)==1,Fraction(2,8)==Fraction(1,4),Fraction(2,5)+Fraction(3,5)==1,Fraction(2,5)+Fraction(3,5)==1,2+3==5,5-3==2,5-3==2,5-3==2,2+3+5==10,5-3==2)
 return tests[list(REL).index(c)]
def main():
 cid,root=sys.argv[1],Path(sys.argv[2]);sealed=json.loads(Path(sys.argv[3]).read_text());r=REL[cid];axes=(("answer-only","complete-positive-electrical-carrier"),("imported-fit-continuum",r),("endpoint-only","complete-interface-field-cycle-state-path"),("condition-erased","specimen-method-field-temperature-uncertainty-held"),("headline-only","complete-trace"),("target-or-prior-model","root-bound-forward-forcing"),("selected-instance","positive-finite-successor-closure"),("fit-exception-extra-rule","no-extra-rule"));gen=tuple("__".join(x) for x in product(*axes));surv="__".join(x[1] for x in axes);received=tuple(x["candidate_id"] for x in sealed["census"]["candidates"]);dec={x["candidate_id"]:bool(x["survives"]) for x in sealed["decisions"]};expected={x:x==surv for x in gen};v=json.loads((root/"experiments/external_sources/materials/elec_001_012_v1/complete_evidence_vector_v1.json").read_text());vid=v.pop("complete_vector_identity");rows={x["claim_id"]:x for x in v["claims"]};evidence=vid==canon(v) and len(rows)==12 and cid in rows and rows[cid]["all_registered_fragments_present"] and all(fh(root/x["snapshot_path"])==x["snapshot_hash"] for x in rows[cid]["comparisons"]);passed=all((received==gen,len(received)==len(set(received))==256,dec==expected,sum(expected.values())==1,sealed["closure"]["scope"]=="depth_independent",len(sealed["controls"])==4,all(x["passed"] for x in sealed["controls"]),native(cid),evidence));print(json.dumps({"validated_seal_hash":sealed["seal_hash"],"recomputed_from_declared_inputs":True,"passed":passed,"certificate":{"claim_id":cid,"candidate_count":len(received),"unique_survivor_count":sum(expected.values()),"native_reconstruction":native(cid),"external_reconstruction":evidence,"closure_scope":sealed["closure"]["scope"],"free_parameter_or_fitted_target_used":False}},sort_keys=True));raise SystemExit(0 if passed else 1)
if __name__=="__main__":main()
