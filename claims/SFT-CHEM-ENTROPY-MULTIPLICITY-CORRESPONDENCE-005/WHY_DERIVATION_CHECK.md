# Chemical entropy and multiplicity correspondence

Claim: `SFT-CHEM-ENTROPY-MULTIPLICITY-CORRESPONDENCE-005`  
Chemistry obligation: `SFT-CHEM-OBL-THERMO-005`

## WHY

A scalar logarithm erases which chemical microstates observation merged and introduces a proof value outside the Smithian exact domain. The native entropy object therefore retains the complete finite support, each disjoint macro-observation class, its exact positive multiplicity and whole-part, and every unresolved pair. A singleton does not receive numerical zero; its unresolved distinction support is structural `EmptyOne`.

## DERIVATION

The complete eight-axis grammar generates 256 alternatives and leaves one survivor:

`complete-finite-chemical-microstate-support__disjoint-exhaustive-chemical-observation-classes__exact-positive-class-count-and-whole-part__complete-unresolved-distinction-ledger__structural-EmptyOne-singleton-certainty__complete-value-free-entropy-phase-identity-seal__complete-13-row-entropy-phase-transition-vector__depth-independent-one-microstate-ledger-successor`

Adding one fresh microstate updates exactly one class, adds one unresolved pair with each prior member of that class, recalculates exact whole-parts and preserves every previous microstate and unaffected class. This closes every finite generated support without a distribution, partition function, fitted probability, logarithm, irrational quantity or continuum.

## CHECK

All thirteen state identities and fourteen column identities seal before scalar entropy, phase or temperature values open. The complete NIST one-bar water surface retains thirteen exact positive entropy values, twelve exact positive increments, nine liquid states, four vapour states and both `372.75593 K` phase-boundary states. The phase entropy jump is `109.102104 J/(mol K)`. Independently, the post-seal phase enthalpy jump divided by the same boundary temperature gives `4066845420/37275593 J/(mol K)`; the two records agree within a bound derived solely from their displayed decimal resolutions.

## FALSIFICATION

The law fails if exact chemical multiplicity requires a fitted distribution or logarithmic proof scalar, if observation classes overlap or omit a microstate, if the complete unresolved-pair ledger is lost, if singleton certainty requires numerical zero, if successor extension changes prior identities, if target data select the law, if any entropy or phase row is omitted, or if the sealed phase-transition comparisons fail.
