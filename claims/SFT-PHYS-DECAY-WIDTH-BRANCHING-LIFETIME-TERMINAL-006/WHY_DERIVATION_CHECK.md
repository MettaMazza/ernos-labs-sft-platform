# SFT-PHYS-DECAY-WIDTH-BRANCHING-LIFETIME-TERMINAL-006: WHY / DERIVATION / CHECK

## WHY

An unstable Fold word does not disappear. It executes a conserved transition
into one of its generated, distinguishable output compositions. The prior
particle-decay law establishes that carrier-preserving transition. The prior
atomic and nuclear laws establish exact transition support, finite recurrence
and observed duration. VI-2 asks what is then forced when more than one output
channel is open:

1. what a partial decay width is;
2. how all partial widths compose into one total width;
3. why branching parts must partition the One;
4. why total width and lifetime are reciprocal carriers;
5. how a closed channel is represented without numerical zero; and
6. whether the complete measured particle vectors retain those relations.

This claim closes all six questions at the declared boundary. It does not
import an ontic random choice, a continuum exponential, a fitted line shape or
a conventional amplitude model. Observational uncertainty is the quotient of
unobserved deterministic predecessor paths; it is not a new dynamics premise.

## DECLARED BOUNDARY

The theorem ranges over every nonempty finite generated family of mutually
distinguishable open decay channels. Every open channel carries:

- one exact positive transition-overlap leg;
- one exact positive generated-output support;
- one exact positive action carrier;
- one exact positive partial width; and
- its complete conserved output record.

A closed channel is the structural empty form `()`. It is absent from the open
family and contributes no numerical object. The grammar therefore never needs
to create a numerical-zero width.

The result is depth-independent. Its certificate consists of a one-channel
base and an arbitrary positive channel-successor step, not an inference from a
finite particle table.

## COMPLETE ENUMERATION

The engine generated the complete product of nine axes:

- 3 partial-width carriers;
- 3 open/closed channel domains;
- 3 total-width aggregators;
- 3 branching normalizations;
- 3 partition laws;
- 3 width-lifetime laws;
- 3 induced orderings;
- 2 target-custody states; and
- 2 extension states.

The product contains exactly

`3 × 3 × 3 × 3 × 3 × 3 × 3 × 2 × 2 = 8,748`

forms. Every form received a trace and a decision. Exactly one survived:

`paired-transition-weight-times-output-support-per-action__positive-open-channels-closed-as-empty__ordered-positive-sum-of-partial-widths__partial-width-over-total-width__complete-exclusive-partition-of-one__action-over-total-width__greater-width-shorter-lifetime__sealed-before-release__empty-extension`

The survivor was computed by the untouched engine. It was not stored in the
claimant, declared by the submission or selected by the measurement target.

## FORCED PARTIAL WIDTH

Let an open channel have transition-overlap leg `a`, generated output support
`s`, and action carrier `h`. All are exact and positive. Observation of a
completed transition has two compatible legs: preparation to transition and
transition to retained output record. Their exact paired weight is `a × a`.
The partial width is therefore

`gamma = a × a × s / h`.

The pairing is load-bearing. At the target-free witness

`a = 1/2`, `s = 3/4`, `h = One`,

the forced width is

`gamma = (1/2)(1/2)(3/4) = 3/16`.

The unpaired alternative gives `3/8` and is rejected. A target-assigned partial
width is rejected because it adds a value not generated by the transition
structure.

This also forces the monotonic directions. With the other carriers held, a
larger paired transition support or a larger available output support produces
a larger partial width; a larger action carrier produces a smaller width per
action.

## FORCED TOTAL WIDTH

Let the nonempty open family be

`(gamma_1, gamma_2, ..., gamma_n)`.

The channels are mutually distinguishable and collectively complete at the
registered boundary. Composition preserves every partial record, so the total
width is the ordered positive sum

`Gamma_n = gamma_1 + gamma_2 + ... + gamma_n`.

The construction starts from `gamma_1`; it does not seed the sum with a
numerical zero. Taking only the largest partial width discards open transition
support. Taking the arithmetic mean changes the total event support when a
channel is duplicated or added. Both alternatives fail conservation and
composition.

The target-free three-channel witness is

`(1/4, 1/2, 5/4)`.

Its total is exactly `2`. The maximum-only candidate is `5/4`; the mean is
`2/3`. Only the complete positive sum retains all three open transitions.

## FORCED BRANCHING PARTS

One observed decay releases exactly one member of the complete mutually
exclusive channel family. The part carried by channel `i` is therefore its
partial width relative to the whole:

`B_i = gamma_i / Gamma_n`.

For the witness above, the exact branching word is

`(1/8, 1/4, 5/8)`.

Its complete sum is

`1/8 + 1/4 + 5/8 = One`.

The inverse ratio `Gamma/gamma_i` is not a part of the One. The raw partial
width is not a branching part because it still carries the dimensional total
scale. An incomplete subset falls short of the One; an overlapping list counts
some predecessor support more than once and exceeds it.

Aggregate and subset labels therefore cannot be appended as if each were a new
exclusive branch. They must retain their containment relation. This becomes a
decisive empirical control in the PDG comparison.

## BASE AND SUCCESSOR PROOF

### Base

One positive open channel has total width `Gamma_1 = gamma_1`. Its only
branching part is

`gamma_1/Gamma_1 = One`.

Its lifetime is `h/Gamma_1`. A closed alternative is the empty form and is not
inserted into the open family.

### Successor

Suppose `n` open channels have positive total `Gamma_n` and branching parts
that sum to the One. Append one generated positive open channel
`gamma_(n+1)`. Composition forces

`Gamma_(n+1) = Gamma_n + gamma_(n+1)`.

The increase is exactly `gamma_(n+1)` and is positive. Renormalizing every
retained channel to the enlarged whole gives

`[gamma_1 + ... + gamma_n + gamma_(n+1)] / Gamma_(n+1) = One`.

No prior channel is erased, no channel is counted twice, and no correction is
available. The theorem therefore holds for every nonempty finite generated
channel family.

## FORCED LIFETIME

Width is completed transition support per action carrier. Duration is the
action carrier required for one complete transition support. The two
organizations are exact reciprocals:

`tau = h / Gamma`.

In normalized action units:

- width `1/2` gives lifetime `2`;
- width `One` gives lifetime `One`; and
- width `2` gives lifetime `1/2`.

For any two exact positive widths with `Gamma_b > Gamma_a`, the positive take

`h/Gamma_a take h/Gamma_b`

exists. Thus the wider state is strictly shorter-lived. The alternatives
`tau = Gamma/h` and `tau = h Gamma` reverse that forced ordering and are
eliminated.

## SEALED EMPIRICAL PROTOCOL

The formal module contains no PDF locator, particle name, measured width,
branching fraction, lifetime, target reader, stored answer key or admission
flag. The capability-closed prediction program can inspect only the exact
formal certificate. It has no filesystem, network, subprocess, clock,
environment, dynamic-import or foreign-function capability.

The program and output are sealed before the external custodian releases the
registered targets. The comparison then opens:

- the official 2026 Particle Data Group W-boson listing; and
- the frozen official NIST CODATA 2022 constants table.

The PDG PDF was frozen at

`sha256:f1905e46980fb732a525de5a1eaa58cb6a87ff97a9dc135e3d7935912ba1d2a1`.

The NIST table was frozen at

`sha256:77fb90e66c40db3e6eb16630bc9c88e4c7c8beddbe5e71be406f2f26e3f67e67`.

## COMPLETE PDG WIDTH CHECK

Printed page 5 defines the listed W width as the mass-dependent
Breit-Wigner width parameter. That is the measurement body's dimensional
carrier; it does not select the Fold composition law.

All sixteen registered rows are retained:

- the PDG average `2.14 ± 0.05 GeV`;
- all three inputs used in that average: ATLAS, LEP and Tevatron;
- all twelve rows explicitly excluded from the PDG average; and
- every symmetric, asymmetric, statistical and systematic uncertainty and the
  reported scale-factor note.

The PDG average gives the exact printed interval

`[209/100, 219/100] GeV = [2.09, 2.19] GeV`.

Nothing is discarded because it is inconvenient. The unused rows remain part
of the source custody record and any omission fails validation.

## COMPLETE PDG BRANCHING CHECK

Printed page 7 contains thirteen primary mode rows. All thirteen are retained.
The table itself labels each mode by `Gamma_i/Gamma`, directly testing the
forced partial-over-total organization.

The comparison preserves the difference between:

- the average per lepton species;
- the mutually exclusive `e nu`, `mu nu`, `tau nu` and hadronic primary
  partition;
- inclusive hadronic subsets such as `c X`;
- exclusive hadronic subsets such as meson-plus-photon modes;
- search classes; and
- upper limits.

Only the mutually exclusive primary channels form the partition. Appending
`c X` to the hadronic branch, for example, deliberately double counts hadronic
support and fails the control.

### Lepton-universality organization

PDG reports one per-species leptonic part

`B_l = 10.86 ± 0.09 percent`

and a hadronic part

`B_h = 67.41 ± 0.27 percent`.

The complete exact interval sum is

`[1989/2000, 10053/10000] = [0.9945, 1.0053]`,

which contains the One. The printed central values sum to `0.9999`; the exact
printed rounding take is only `1/10000`.

More strongly, PDG explicitly states its fitted closure relation:

`B(W to hadrons) = One take 3 B(W to ell nu)`.

Using the independently opened printed leptonic central value, the Fold law
forces

`One take 3(543/5000) = 3371/5000 = 0.6742`.

That forced hadronic value lies inside the independently reported PDG interval
`[0.6714, 0.6768]`.

### Individual-lepton organization

The separately fitted complete vector is

`(1071/10000, 1063/10000, 569/5000, 6741/10000)`

for electron, muon, tau and hadrons. Its exact interval sum is

`[4967/5000, 2523/2500] = [0.9934, 1.0092]`,

which also contains the One. The rounded central vector exceeds the One by
`13/10000`; that signed appearance is not inserted as a negative proof object.
The exact positive rounding take is recorded instead.

Using only the three independently opened leptonic central parts, closure
forces the hadronic complement

`One take (1071/10000 + 1063/10000 + 569/5000)`

`= 841/1250 = 0.6728`.

This second forced value also lies inside the PDG hadronic interval. Thus two
different measured organizations reproduce the same exact Fold partition law.

## PARTIAL-WIDTH TRANSPORT

Combining the complete individual branch intervals with the independently
reported total-width interval gives exact partial-width enclosures:

- electron: `[44099/200000, 238053/1000000] GeV`;
- muon: `[27379/125000, 118041/500000] GeV`;
- tau: `[233453/1000000, 253821/1000000] GeV`; and
- hadrons: `[701613/500000, 92637/62500] GeV`.

Their exact summed enclosure is

`[1038103/500000, 552537/250000] GeV`

`= [2.076206, 2.210148] GeV`,

which encloses the independently reported total-width interval
`[2.09, 2.19] GeV`. This comparison uses every primary channel and propagates
every registered uncertainty.

## NIST ACTION AND LIFETIME TRANSPORT

NIST records the reduced action carrier as

`6.582 119 569... × 10^-16 eV s`.

The ellipsis is not declared to be a finite exact rational. The printed prefix
is enclosed between adjacent exact rational decimal endpoints:

`[6582119569/10^25, 6582119570/10^25] eV s`.

After exact unit transport to GeV seconds and composition with the PDG width
interval, the forced lifetime enclosure is

`[6582119569/21900000000000000000000000000000000,`

` 658211957/2090000000000000000000000000000000] s`,

approximately

`[3.00553404977 × 10^-25, 3.14933950718 × 10^-25] s`.

Every endpoint is positive and finite. Using the retained ATLAS and Tevatron
central widths independently reproduces the forced ordering: the greater ATLAS
width gives the shorter transported lifetime.

This is an operational action-width transport check. It does not pretend that
PDG printed a separately measured W lifetime scalar. The universal reciprocal
law is forced before measurement; PDG and NIST supply independent dimensional
carriers after the seal.

## UNFAVOURABLE CONTROLS

The claim fails closed under each of the following deliberate alterations:

- omit the hadronic primary channel;
- add the inclusive `c X` subset as another primary channel;
- invert `Gamma_i/Gamma` to `Gamma/Gamma_i`;
- make lifetime proportional to width;
- remove any one of the sixteen width rows;
- alter the PDG or NIST source hash;
- expose targets before the prediction seal;
- duplicate a candidate identity; or
- add a free correction after closure.

All formal and empirical controls passed. The five focused tests passed once.
No repeated heavy validation was used.

## WHAT THE RESULT MEANS

The result unifies atomic, nuclear and particle decay at the level that is
forced by their common Fold transition structure. A spectral line width, a
nuclear transition rate and an unstable-particle width are not unrelated
definitions: they are the same exact organization of paired transition support
per action carrier. Multiple channels then have only one lawful composition:
positive partial widths sum, ratios partition the One, and reciprocal action
gives duration.

Particle-specific channel magnitudes are complete physical carriers generated
by the particle's already derived state, coupling and available output support.
The theorem forces how those carriers must compose and how a measured vector
can validate or invalidate the law. The PDG values are not retrofitted into the
candidate grammar.

## ENGINE RESULT

- Engine receipt:
  `sha256:a4e14f20a7c1f6920115f0e5ad0b911b5462b690aad35ee79aac39b691d12d3f`.
- Derivation seal:
  `sha256:42d6264dd3d0731a3920f4c904f688ccf53e028f356a0be3ff910c6041dc0f6d`.
- Independent validation:
  `sha256:72baee80b3adaeac88de833ce60ce9ab2f0c8885919a6d53aadc763fd5a44068`.
- Independent certificate:
  `sha256:29c5bc317aac67c022d87e417828f668fd1d0748855e6d0eab968f7047c00910`.
- Empirical validation:
  `sha256:5f55eaf606e42e1746e073343e52e184ebe55bd62288aa5c0339e4834511eef7`.
- Measurement receipt:
  `sha256:d48b51e08f77bfd7850e90dcc4f0bea4b17a68f8588730baa1d0627dd9376f36`.
- Closure: `depth_independent`.
- Engine gates: nine of nine passed.
- Violations: none.

## EXCLUSIONS

- no V1/V2 executable, certificate, answer table or survivor identity as a premise
- no particle name, measured width, branching fraction, lifetime or source locator in formal candidate decisions
- no claimant-supplied expected survivor or submission-controlled admission flag
- no numerical-zero state, negative proof value, irrational, imaginary, transcendental or floating proof magnitude
- no continuum exponential, ontic random choice or imported stochastic collapse
- no imported Breit-Wigner dynamics, perturbative decay amplitude or fitted coupling
- no incomplete exclusive partition or double-counted subset
- no promotion of rounded decimal inscriptions or ellipses to exact formal values
- no target access before the prediction seal
- no free correction after closure
