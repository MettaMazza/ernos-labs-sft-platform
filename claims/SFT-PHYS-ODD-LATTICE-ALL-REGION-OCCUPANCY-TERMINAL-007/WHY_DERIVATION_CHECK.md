# Why this is a derivation check

The admitted result is not the old E3 Boolean restated. The V3 claimant never reads the superseded source and never receives its lattice size, Fold count, region count, measured vector or answer label.

The formal grammar generates all 2,916 combinations across eight registered axes: lattice domain, Fold action, support scope, region partition, occupancy relation, depth closure, target custody and extension. Every form is decided. Exactly one remains: the complete positive odd lattice, exact binary-residue Fold permutation, complete support, complete positive-region partition, exact invariant vector, arbitrary positive-depth successor, sealed target and empty extension.

The decisive structure is parity. If two odd-lattice source indices had the same Fold image, the odd lattice count would divide twice their strict sub-lattice gap. Because the lattice count shares no binary factor, it would divide the gap itself; that is impossible unless the source indices are identical. The finite image has the same cardinality as the source, so the Fold is a permutation. Repeating a permutation preserves complete support at every positive successor depth. A complete region tally depends on membership, not ordering, so its full vector is invariant. When the region count does not exceed member count, every exact positive region contains a member.

The implementation-distinct validator imports neither claimant nor post-seal adapter. It regenerates all 2,916 candidate identifiers, recomputes all decisions, independently implements odd-residue Fold action and region tallies, verifies multiple complete odd lattices and depths, and confirms the unique survivor and every engine control.

Only after the prediction seal does the empirical adapter release the two byte-identical V1 files. It does not execute them. A separate exact-rational implementation reconstructs their explicit procedure and retains every coordinate: 255 complete members, twelve Fold steps and eight regions yield `(32,32,32,32,32,32,32,31)`. The vector totals 255, occupies all eight regions and equals the independently measured first- and eleventh-step vectors. Thus the original finite observation is reproduced while the V3 result closes the general positive-depth law.

The frozen engine—not the claimant—computed the surviving candidate and issued admission only after registration, complete enumeration, forcing, form closure, hostile controls, sealing, independent validation, post-seal empirical validation and model-admission gates all passed.
