# Why this is a derivation check

Exact complement has one generated fixed point: the half-One. The grammar includes and rejects the tempting but invalid inference that reflection symmetry alone places every member of a symmetric set on its axis. This closes the SFT mirror theorem while retaining the V1 boundary against an unproved classical complex-zero claim.
