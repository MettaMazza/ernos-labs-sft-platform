# Fault-tolerance boundary clarification - 2026-07-24

The existing receipt remains unchanged. Its phrase "unlimited thresholds"
refers only to unregistered physical hardware noise thresholds.

The mathematical law is already unbounded over supplied positive finite fault
allowances: the error-correction chain forces width `2t+1` and carries a
positive-successor certificate for every generated finite `t`. The
authoritative V2 Step 407 states this distinction explicitly and constructs the
successor that defeats any proposed fixed positive finite ceiling.

No infinite-width object, stochastic hardware rate or device threshold is
claimed.
