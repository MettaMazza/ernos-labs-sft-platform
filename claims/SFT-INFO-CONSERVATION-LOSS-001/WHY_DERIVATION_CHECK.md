# Exact information conservation, loss and transformation

Claim: `SFT-INFO-CONSERVATION-LOSS-001`

## Why

Information conservation and loss require pair-level accounting: image counts alone cannot show which source alternatives remain recoverable or what record reversal requires.

## Derivation

Encoding supplies inverse classes, channels supply relations, mutual information supplies joint incidence and dynamics supplies composition. Ten axes force exhaustive pair accounting and the compositional loss law.

The registered grammar boundary is:

> All finite deterministic information transformations generated from complete canonical source support, total single-valued maps, exact image fibres, complete source-pair ledgers and transformation composition.

The complete product has `1024` candidates across
`10` declared structural dimensions. Every dimension has one
all-preserving coordinate and one explicit failure coordinate. Exactly one
product member combines all preserving coordinates.

Admitted coordinates:

- `support` — admitted `complete-canonical-source`: Every registered source form occurs once.
- `map` — admitted `total-single-valued-map`: Each source has exactly one held image.
- `provenance` — admitted `exact-predecessor-fibres`: Every image retains its complete source class.
- `accounting` — admitted `complete-source-pair-partition`: Every source pair is classified exactly as retained or closed.
- `loss` — admitted `non-singleton-image-fibre`: Loss is the exact source distinctions closed inside an image fibre.
- `reversal` — admitted `singleton-fibre-or-held-record`: Singleton fibres reverse directly; merged fibres require retained predecessor labels.
- `composition` — admitted `retained-pair-subset`: A downstream map can preserve or close an upstream distinction but cannot reopen it.
- `conservation` — admitted `retained-plus-closed-exhaustion`: The two exact ledgers disjointly exhaust all source distinctions.
- `generality` — admitted `source-successor-map-cell`: A fresh source adds one held map cell and all new pair comparisons.
- `addition` — admitted `no-extra-information-source`: All output distinctions trace to separated source images or declared external input.

The admitted result is:

> The transformation kernel is complete source support, a total held map, exact predecessor fibres, exhaustive retained/closed source distinctions, singleton-fibre reversibility, held reverse records, compositional retention monotonicity and no unrecorded information source.

Its operational laws are:

- retained and closed ledgers are disjoint and exhaust every source distinction.
- information loss is exactly the source distinctions inside non-singleton image fibres.
- a transformation is reversible exactly when every predecessor fibre is singleton.
- a non-singleton fibre requires a retained predecessor label for exact reconstruction.
- composition cannot restore a distinction already closed by an upstream deterministic map.

Depth-independent closure uses this base:

> One source maps to one held image, has one singleton fibre and structural empty-One pair ledgers.

and this successor certificate:

> Adding one fresh source adds one image membership and one pair against each prior source. Image equality places each new pair exactly in the retained or closed ledger and updates one predecessor fibre.

The exact exclusions are:

- no image without source provenance.
- no assumed inverse across merged predecessors.
- no scalar conservation law replacing distinction ledgers.
- no downstream reopening without a declared external record.

## Check

Execute all 1,024 kernels; compare identity, prefix projection and total erasure, verify exact pair ledgers, reverse records and downstream subset law, then independently regenerate the product.

Operational witnesses:

- `identity-conservation`: Identity retains all six source distinctions and closes none.
- `prefix-loss`: Prefix projection retains four cross-prefix distinctions and closes two exact predecessor pairs.
- `reversibility-boundary`: Identity is reversible while prefix projection is not.
- `reverse-record`: Each prefix image retains its two possible source labels.
- `data-processing`: Erasing the retained prefix cannot reopen either previously closed distinction.

Four required controls reject a false premise, changed source identity, altered
survivor set and excluded boundary import. The separate validator independently
regenerates the literal candidate product and sole survivor without importing the
scientific law module.

## Boundary

This theorem closes exact finite deterministic information transformation. Thermodynamic energy costs and operational quantum measurement belong to later physical and quantum branches.

Conventional correspondence terms, admitted only after the derivation is sealed:
data processing, information loss, reversible map, many-to-one map, sufficient record.
