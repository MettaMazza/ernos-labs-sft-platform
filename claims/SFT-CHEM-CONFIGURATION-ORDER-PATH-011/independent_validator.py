from itertools import product
import json,sys
CLAIM='SFT-CHEM-CONFIGURATION-ORDER-PATH-011';DOMAINS=(('coordinate-only-surface', 'retained-molecular-carrier'), ('continuum-point-premise', 'generated-configuration-nodes'), ('signed-or-floating-energy', 'exact-positive-order-or-EmptyOne'), ('named-stable-structure', 'local-minimum-by-complete-neighbours'), ('imported-saddle-formula', 'local-barrier-by-complete-neighbours'), ('endpoint-only-reaction', 'connected-complete-configuration-path'), ('open-curve-assumption', 'exact-periodic-endpoint-identity'), ('selected-minima-and-maxima', 'complete-favourable-and-adverse-vector'));SURVIVOR='retained-molecular-carrier__generated-configuration-nodes__exact-positive-order-or-EmptyOne__local-minimum-by-complete-neighbours__local-barrier-by-complete-neighbours__connected-complete-configuration-path__exact-periodic-endpoint-identity__complete-favourable-and-adverse-vector'
def main():
 d=json.load(open(sys.argv[1]));g=["__".join(x) for x in product(*DOMAINS)];r=[x["candidate_id"] for x in d["census"]["candidates"]];z={x["candidate_id"]:x["survives"] for x in d["decisions"]};passed=d["claim_id"]==CLAIM and r==g and len(set(g))==256 and z=={x:x==SURVIVOR for x in g} and sum(z.values())==1 and d["closure"]["scope"]=="depth_independent" and all(x["passed"] for x in d["controls"]);print(json.dumps({"validated_seal_hash":d["seal_hash"],"recomputed_from_declared_inputs":True,"passed":passed,"certificate":{"claim_id":CLAIM,"candidate_count":len(g),"survivor":SURVIVOR if passed else None,"successor":"append-one-adjacent-configuration"}},sort_keys=True))
if __name__=="__main__":main()
