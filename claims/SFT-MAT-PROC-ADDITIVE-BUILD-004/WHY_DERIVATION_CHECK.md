# Why PROC-004 requires a derivation check

An additive build is the exact ordered word of layer, melt-pool and powder-batch identities together with the complete process path.

The 256-form grammar is generated before target release, independently reconstructed and compared against every registered authoritative record.
