# Why this is a derivation check

The formal commit predates access to the registered PDG and lattice-QCD target
record. Its complete 2,916-form grammar is generated from admitted V3
dependencies, and the untouched engine independently computes the sole
survivor. The capability-closed prediction executor cannot read files, the
network, the clock, the environment or subprocesses. Only after its output and
trace are sealed does the target vault release all 47 comparison rows.

The exact result uses positive finite Fold records only: the empty correction
and fixed-point records are structural absence, never numerical zero. The
strong successor is `F + 2`, so its fixed-point exclusion and witness above
every positive finite bound are depth-independent and require neither a
completed infinity nor a measured scalar.

The external comparison is deliberately two-sided. Non-abelian gluon
self-interaction and the linear unbroken string branch agree with the sealed
law. Dynamical quark-pair creation, broken-string mixing and full-QCD
ground-state saturation are also retained, preventing the formal branch from
being overclaimed as the complete physical ground-state potential.
