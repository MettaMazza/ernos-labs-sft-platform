# Conditional conventional decision-family transport law

Claim: `SFT-COMP-CPLX-CONVENTIONAL-TRANSLATION-003`

## Why

The native equality `P_F=NP_F` cannot be exported by analogy.  A complexity
class statement transports only after the source family, encoding, verdict,
certificate and resource carriers have all been made common and exact.

## Derivation

The admitted model-equivalence law supplies bidirectional step simulation.  The
reduction law supplies answer preservation.  Input-size and bound laws supply
comparable resources.  The verification law supplies sound and complete
certificate transport.  Their conjunction forces the conditional transport
kernel and also proves its boundary: omit any relation and the broader result
does not follow.

The complete grammar has eight binary axes and 256 candidates.  The unique
survivor retains:

- complete registered source support;
- total reverse-reconstructible encoding;
- exact bidirectional verdict preservation;
- complete stepwise trace translation;
- sound-complete bidirectional certificate mapping;
- an explicit common size carrier with unit polynomial overhead;
- a prepend-label successor certificate; and
- a conditional family conclusion only.

At depth One, each external label maps to one native fibre label, preserves its
verdict and carries one evaluator/verifier edge.  Prepending a label adds one
source position, one mapped label and one trace edge while retaining every prior
suffix relation.  The complete external family executes through depth seven;
the structural successor executes through depth fourteen.  A changed encoding
breaks reverse identity and the verdict diagram.

## Check

The official execution enumerates and decides all 256 candidates, requires one
survivor, runs all four adverse controls and retains the operational witnesses.
The independent validator separately regenerates candidate order, decisions,
closure and control classes without importing the scientific law module.

## Exact limitation

This closes the operational translation boundary and one complete registered
external decision family.  It does not claim conventional `P=NP` for arbitrary
languages, Turing machines, encodings or polynomial conventions that lack the
required translation certificate.
