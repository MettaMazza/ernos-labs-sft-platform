# Three-dimensional Fold stability

Claim: `SFT-PHYS-SPACE-DIMENSION-THREE-001`

## Why

Spatial dimension must be forced structurally before a physical falloff law is
tested. An observed three-dimensional target is not a derivational premise.

## Derivation

The two Fold fibres provide the strict lower stability boundary: retaining a
return distinct from both roles requires a spatial count beyond two. Their
complete ordered pair-cell support provides the strict upper boundary four:
stability retains a positive restoring remainder before saturation. Exhausting
all positive counts in the open interval leaves only three. The independently
derived second Fold generator is also three, so the recurrence and stability
routes cross-lock.

Eight binary axes and one four-way value axis generate 1,024 forms and retain
only the complete open-window, generator-locked result.

## Check

Both implementations enumerate the complete interval, reject both endpoints
and verify exact equality with generator three. Measurement can subsequently
test the sealed count but cannot select it.
