# Chemical-potential-equivalent component law

Claim: `SFT-CHEM-CHEMICAL-POTENTIAL-EQUIVALENT-COMPONENT-008`  
Chemistry obligation: `SFT-CHEM-OBL-THERMO-008`

## WHY

SFT does not import a signed chemical-potential scalar, logarithm, activity coefficient or grand-canonical parameter. The forced equivalent is operational: the complete exact marginal account for adding one held component to a named phase at one held finite environment. The account retains its exact positive energy increment and exact positive count of distinctions closed by the addition.

## DERIVATION

The eight-axis grammar generates 256 alternatives and leaves one survivor:

`complete-distinct-phase-component-addition-accounts__same-held-component-and-fixed-environment__paired-one-component-exchange-conserving-total-carrier__exact-positive-energy-and-closed-distinction-increments__strict-product-order-with-EmptyOne-equilibrium__complete-value-free-74-row-identity-seal__complete-four-system-74-row-VLE-vector-with-eight-endpoints__depth-independent-common-context-successor`

Paired departure and arrival conserve the component carrier. Strict product order directs exchange only when one complete marginal account is no greater on both axes and strict on at least one. Exact equality is equilibrium with structural `EmptyOne` separations. Crossed accounts halt rather than receiving an arbitrary scalar weight. Adding the same exact positive context to both phase accounts preserves the relation at every finite depth.

## CHECK

A value-free specification was sealed before the selected NIST TRC ThermoML JSON opened. The raw source is preserved byte-for-byte. Every paired experimental binary VLE record is retained: four binary systems, 74 matched interior equilibrium rows at 101.3 kPa, all temperatures, liquid and gas compositions, compound identities, phases, uncertainties and methods. Eight unmatched pure-component temperature endpoints and all five pure-source datasets remain in the source record rather than being discarded.

Every liquid and gas composition is reconstructed as two exact positive fractional parts exhausting the One. All 74 equilibrium rows have unequal liquid and gas bulk compositions, directly testing that equilibrium is equality of the held component-exchange account—not equality of phase composition. The external phase-composition vector preserves 66 gas-greater and 8 liquid-greater rows and the observed crossing in the first system. These measured relations are post-seal correspondence records and never select or numerically parameterize the law.

## FALSIFICATION

The law fails if component identity, environment or total carrier is not preserved; if direction requires a fitted scalar, logarithm, activity coefficient or tie-break; if equal accounts do not yield equilibrium; if equal accounts are incorrectly made to require equal bulk compositions; if common context changes the relation; if any target opens before the complete identity seal; or if any source system, row, endpoint, uncertainty or adverse orientation is omitted.
