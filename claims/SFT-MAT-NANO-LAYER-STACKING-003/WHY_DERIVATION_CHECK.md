# Why NANO-003 requires a derivation check

A two-dimensional material stack is the exact ordered word of layer identities, adjacent interfaces and registry labels; no layer or interface may be erased.

The 256-form grammar is generated before target release, independently reconstructed and compared against every registered authoritative record.
