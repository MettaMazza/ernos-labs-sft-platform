# WHY

The earlier reconstruction correctly identified a binary scale tower, a
generator-indexed running family and a leading electroweak curve, but it did not
yet put the full-support level, held terminal level, exact terminal return,
unit invariance and complete measured running vectors into one admitted law.
This successor closes that categorical Physics obligation without treating a
measured energy as a rung selector.

The distinction is decisive.  Complete support and active physical support are
not always identical.  At the terminal electroweak boundary, complete support
is sixteen, while the three generator directions are held; the active level is
therefore thirteen.  This resolves the earlier lower-order `level = 2^d`
identification without deleting it or fitting a new level to the measured weak
angle.

# DERIVATION

The scale axis begins at the One.  Every Fold-depth successor doubles complete
support:

`1, 2, 4, 8, 16, 32, 64, 128, ...`.

The corresponding exact spacing is its positive reciprocal.  A local carrier
acts once per cell, so traversal count, Fold support and the native running
coordinate are one generated axis rather than three independent inputs.  The
complete forced sector ladder `(2,3,5,7)` uses this same support.

For binary electroweak support `R`, the charged holding carrier is
`(R+1)/(R+2)` and the neutral carrier is half-One.  Squared channel support
therefore forces

`w(R) = (R+2)^2 / [4(R+1)^2 + (R+2)^2]`.

Its first four binary-support values are exactly `9/25`, `4/13`, `9/34` and
`25/106`.  Cross multiplication shows the difference between `w(R)` and
`w(2R)` has positive numerator `R(4R^2+9R+4)`, so every successor lowers the
share without negative proof values or a continuum derivative.

Terminal promotion has four rungs and complete binary support sixteen.  Holding
the three generator directions forces active level thirteen.  Thus the terminal
base is `w(13)=225/1009`.  The unique complete-support successor is seventeen,
and the already admitted sole terminal electromagnetic return contributes
`alpha_terminal/17`.  The result is exactly

`1930922298157999/8642477221479757`,

the independently admitted terminal on-shell share.  No measurement occurs in
this calculation.

The binary-sector source `2+R` is itself a Fold power only at support two.  At
every support from four onward it factors into two times an odd count above the
One, excluding every later binary power by a depth-independent certificate.

A unit is a held reference.  Multiplying numerator and denominator of a
like-dimension ratio by the same exact positive rational reference cancels it
identically.  The proton-Planck law supplies the exact physical scale ratio;
only after sealing may one measured reference supply a conventional dimensional
name.  The unit never selects the axis, rung or law.

The complete grammar has 12,288 forms across thirteen axes.  It includes
numerical-zero origins, continuous or free steps, untyped spacing, independent
running axes, selected sectors, unsquared or target-assigned weak angles,
unheld terminal support, target-selected anchors, law-changing unit names,
measurement-selected rungs, early target access, false historical blindness
and free extensions.  Exactly one form preserves every dependency and boundary.

# CHECK

The implementation checks all generated supports, reciprocal spacings, local
traversals, sector vectors, leading weak shares, terminal held-support chain,
unique internal anchor, named landmarks and exact common-unit cancellation.
A separate implementation regenerates all 12,288 candidates and the unique
survivor without importing the claimant or any target.

Only after the prediction seal does the target custodian release:

- all four PDG 2026 weak-angle scheme rows;
- all four explicit low-transfer numerical rows;
- the complete PDG weak-running figure by source hash, without invented plot
  coordinates;
- the previously sealed complete QCD and electromagnetic running vectors;
- the terminal on-shell electroweak receipt; and
- the terminal proton-Planck hierarchy receipt.

The exact terminal prediction lies inside the PDG on-shell interval.  The exact
support-eight leading value lies inside the registered cesium APV interval as a
post-seal correspondence, not as a selector.  The complete E158 and APV
intervals lie above the MS Z interval, matching the sealed sub-W running
direction.  The eDIS interval remains non-discriminating.  The NuTeV on-shell
interval remains an explicit adverse row, with the source's interpretation
concerns retained.  The PDG W-threshold minimum and sign change are also
retained, preventing an overclaim of one monotone curve across all energies.

Changing the on-shell target to a disjoint interval rejects the empirical
correspondence.  Any changed source, receipt, candidate, seal or target-access
order halts admission.
