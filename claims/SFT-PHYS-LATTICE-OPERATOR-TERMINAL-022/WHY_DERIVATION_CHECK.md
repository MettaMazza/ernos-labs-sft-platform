# WHY

This claim closes the exact finite operator family needed to connect local Fold
transport, causal propagation and phase modes in one, two and three generated
spatial dimensions.  It uses finite rational weights rather than importing a
continuum differential operator or irrational mode value.

# DERIVATION

At dimension `d`, half-One remains at the centre and half-One is shared exactly
over the `2d` nearest neighbours.  The point-source opposition ring therefore
has positive magnitude `2d`.  Repeated one-cell transport generates the exact
finite causal balls recorded for dimensions one, two and three.  On an
`N`-site cycle, the generated phase labels give exactly `N` modes including one
stationary identity mode; phases are typed labels, not imaginary proof scalars.

# CHECK

The complete 1,024-form grammar produced one unique survivor.  Independent
reconstruction checks the stencils, normalization, point-source rings, causal
balls and finite mode count.  Adverse controls passed.  Existing sealed
dispersion, Maxwell and cubic-support results supply downstream comparison.
Frozen-engine receipt:
`sha256:2d54a071b1a994f26b3a05083d0d57ec33659b9d5430b17467f5dfd03d525fb8`.
