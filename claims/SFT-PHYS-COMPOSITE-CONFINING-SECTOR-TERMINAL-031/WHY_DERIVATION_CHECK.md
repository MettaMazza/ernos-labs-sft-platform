# Why / Derivation / Check

## Why this result matters

The composite-sector rows in the earlier corpus contained exact mode counts,
recurrence cycles, complementary pairs and holding fractions.  A clean rebuild
cannot retain those rows by copying their table, naming their physical
interpretation, or using a measured coupling to choose a pattern.  It must
explain why the entire construction is forced and why the same construction
continues beyond the five previously recorded cases.

This claim supplies that general law.  It proves that every positive finite
even Fold sector beyond the binary fibre has an odd held predecessor support.
That parity makes Fold doubling exactly invertible on every nonempty held
label.  Confinement, recurrence, the disjoint orbit partition, complementary
pair closure, the whole pair count and the all-but-one holding coupling then
follow from the same structure.  The five earlier rows are generated instances
of the law, not premises selecting it.

## Derivation from admitted dependencies

Let `s` be any generated positive finite even sector beyond two.  The unique
held predecessor is

`d = s - 1`.

Because `s` is even, `d` is odd.  The complete nonempty standing support is the
exact set of labels

`1/d, 2/d, ..., (d-1)/d`.

Fold acts on a numerator label by exact doubling and casting complete held
denominators:

`F(k) = 2k mod d`.

No nonempty label maps to the empty label because `d` is odd.  More strongly,
the positive whole `(d+1)/2` is an exact inverse multiplier:

`((d+1)/2) * 2k = k mod d`.

Fold therefore permutes the entire nonempty support and preserves the held
denominator.  A permutation of a finite support has one and only one disjoint
first-return orbit partition.  No mode can escape the denominator and no mode
can be omitted or duplicated.

Every label `k` has the unique positive complement `d-k`.  Their exact sum is
`d`, so the two fractions reassemble the One.  Odd `d` forbids a self-pair.
The `d-1` nonempty labels consequently form exactly

`(d-1)/2 = (s-2)/2`

complementary pairs.  This is the exact whole formula.  The earlier shorthand
`(s-1)/2` would not be whole-valued for even `s` and is therefore not retained.

Holding every predecessor part over the completed sector gives the unique
coupling

`d/s = (s-1)/s`.

No measured value, named force, stored orbit table, fitted coefficient or
extra orbit rule enters any survivor decision.

## Exhaustive enumeration

The registered grammar contains eleven binary alternative axes and therefore
exactly `2^11 = 2,048` candidates.  The axes separately test:

1. selected cases against the arbitrary-even-sector law;
2. measured denominators against the unique held predecessor;
3. selected modes against complete nonempty support;
4. imported permutations against Fold doubling;
5. table assertions against the odd-support inverse;
6. overlapping named orbits against the complete first-return partition;
7. borrowed pairing against unique One complements;
8. the ambiguous half count against the exact whole count;
9. fitted couplings against the all-but-one share;
10. measurement overclaim against the sealed comparison boundary; and
11. inserted corrections against structural exhaustion.

Every candidate is generated and decided.  Exactly one candidate preserves all
eleven requirements.  The closure is depth-independent because the inverse and
finite-permutation argument applies to every generated positive finite even
sector, rather than only to the five registered examples.

## Exact generated census

| Sector `s` | Held `d` | First-return orbit sizes | Antipodal pairs | Coupling |
|---:|---:|---|---:|---:|
| 8 | 7 | `(3, 3)` | 3 | `7/8` |
| 12 | 11 | `(10)` | 5 | `11/12` |
| 18 | 17 | `(8, 8)` | 8 | `17/18` |
| 24 | 23 | `(11, 11)` | 11 | `23/24` |
| 30 | 29 | `(28)` | 14 | `29/30` |

The exact numerator orbits are preserved in the candidate census and
independent certificate.  The already admitted post-seal force-sector anchor
checks the eight-carrier correspondence after derivation.  The remaining
forced sectors are standing predictions and cannot be relabelled as measured
confirmations without a separately registered empirical protocol.

## Machine and independent checks

- Candidate count: `2,048` complete and uniquely identified candidates.
- Closure: `depth_independent`.
- Required controls: false premise, tampered source, tampered artifact and
  grammar boundary; every control passed.
- Independent implementation: recomputed the survivor, five exact orbit
  partitions, pair counts and couplings from the sealed input.
- Source manifest:
  `sha256:fdfab054033e97c405ab0a5d9237425c3b5ee4acaf1e3ccc154e47160c5454ab`.
- Derivation seal:
  `sha256:f2a635c4e730cc95000af2f718ae9c0ea451904511d39264b7aa4669be821395`.
- Independent validation:
  `sha256:a4b6ff145b49bdf9e7752f6b380faa2559e021103010f6efd6932681e2fe9e7e`.
- Engine receipt:
  `sha256:eb8b53e1662db23eff80d371b5b6db094e3c7d4665b5ea0a6882ba3072e29ec8`.

The result is admitted as a formal depth-independent theorem with an inherited
post-seal discriminator for the eight-carrier row.  Its lawful extension
boundary remains open to additional empirically registered correspondences;
the structural law itself contains no empirical selection parameter.
