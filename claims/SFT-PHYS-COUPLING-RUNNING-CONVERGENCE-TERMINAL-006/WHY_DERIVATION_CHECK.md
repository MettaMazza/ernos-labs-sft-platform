# SFT-PHYS-COUPLING-RUNNING-CONVERGENCE-TERMINAL-006: WHY / DERIVATION / CHECK

## WHY

V1 VI-3 did not merely say that a coupling changes. It recorded a stronger obligation: derive the running source from the common depth carrier, derive the holding share at every generated depth, prove that every forced sector follows one exact form, prove that the gaps close as depth advances, and compare the result with measured scale dependence without importing a conventional beta function or allowing the measurements to select the law.

V3 had already sealed two directional components:

- the colour carrier self-sources, fixing the strong-sector range orientation; and
- vacuum screening is progressively removed by closer electromagnetic probing, fixing the electromagnetic exposure orientation.

Those results did not yet close the exact generator-indexed functions, their shared scale transport or constructive convergence. This claim closes that remaining obligation. It does not reopen the earlier directions and it does not erase their receipts.

The required result must satisfy all of the following at once:

1. use the complete already forced prime-sector ladder, not a hand-selected familiar subset;
2. use the generated Fold depth carrier, not an imported continuous energy coordinate;
3. retain an exact positive holding share and exact positive shortfall at every finite depth;
4. prove the successor direction algebraically;
5. derive every ordered sector gap exactly;
6. prove gap closure for arbitrary generated depth and for every finite positive tolerance;
7. retain the different physical coordinate orientations of self-source accumulation and screening removal;
8. seal before authoritative target release;
9. introduce no free correction after closure.

## DERIVATION

### Dependency chain

The engine registration traces this claim through twelve admitted dependencies to the root theorem. The critical carriers are:

- `SFT-PHYS-FORCE-PRIME-SECTOR-LADDER-002`: the complete finite sector ladder is `(2, 3, 5, 7)`;
- `SFT-PHYS-FORCE-COMPLETE-SECTOR-INVENTORY-003`: sector `p` already carries the exact support-successor function;
- `SFT-PHYS-STRONG-RUNNING-DIRECTION-002`: colour self-source support fixes the strong physical orientation;
- `SFT-PHYS-VACUUM-POLARIZATION-RUNNING-003`: screening removal fixes the electromagnetic physical orientation;
- `SFT-PHYS-MEAS-BOUNDARY-GROWTH-001`: a generated boundary grows by counted successor;
- the custody, uncertainty and measured-value laws: targets may open only after the formal seal and must remain complete;
- the potential-infinity and exact-convergence laws: an arbitrary finite successor process and exact rational error certificate are admitted, but a completed infinity is not;
- exact arithmetic and combinatorics: every candidate and every rational identity is generated and counted.

No measurement, conventional renormalization-group equation, beta coefficient, unification group or known coupling value is a dependency.

### Complete grammar

The claimant generates the full Cartesian product of nine axes:

| Axis | Alternatives | Forced survivor |
|---|---:|---|
| sector domain | 3 | complete prime sectors through seven |
| scale generation | 3 | One-base binary depth successor |
| coupling form | 3 | holding share of sector plus support |
| successor law | 3 | binary support raises the holding share |
| pair-gap law | 3 | exact generator gap over paired sources |
| convergence law | 3 | finite positive tolerance witness for every pair |
| physical translation | 3 | carrier-specific self-source range and screening exposure |
| target boundary | 2 | sealed before release |
| extension | 2 | empty extension |

The exact candidate count is

`3 x 3 x 3 x 3 x 3 x 3 x 3 x 2 x 2 = 8,748`.

Every candidate is materialized in `candidate_census.json`. Every decision and rejection reason is materialized in `elimination_receipt.json`. The engine independently found exactly one survivor:

`complete-prime-sectors-through-seven__One-base-binary-depth-successor__holding-share-of-sector-plus-support__binary-support-successor-raises-holding-share__exact-generator-gap-over-paired-sources__finite-positive-epsilon-witness-for-every-pair__carrier-specific-self-source-range-and-screening-exposure__sealed-before-release__empty-extension`

The submission did not provide that identifier to the engine as an expected answer. The identifier exists in the final record because the engine computed and stored it.

### Generated common scale

Let the first finite scale support be the One:

`R_1 = 1`.

One Fold successor doubles the available support:

`R_(n+1) = 2 R_n`.

Thus the exact generated witnesses begin

`1, 2, 4, 8, 16, 32`.

This is a one-based generated sequence. Numerical zero is neither its origin nor a hidden state. Every reached support is finite and positive. The successor may continue without a greatest finite rung, but no completed infinity is admitted as an object.

### Generator-indexed running share

For forced sector `p` and generated support `R`, the source count is

`s_p(R) = p + R`.

One position is the returning One. The held portion is therefore the positive predecessor share

`g_p(R) = [s_p(R) take One] / s_p(R)`

and hence

`g_p(R) = (p + R - 1) / (p + R)`.

Its remaining positive shortfall from the One is exactly

`One take g_p(R) = 1 / (p + R)`.

No coefficient is fitted. Sector identity supplies `p`; generated depth supplies `R`; the returning One supplies the unique shortfall.

At the base support, the complete vector is

| `p` | `g_p(1)` |
|---:|---:|
| 2 | `2/3` |
| 3 | `3/4` |
| 5 | `5/6` |
| 7 | `7/8` |

At support eight, the vector is

| `p` | `g_p(8)` |
|---:|---:|
| 2 | `9/10` |
| 3 | `10/11` |
| 5 | `12/13` |
| 7 | `14/15` |

### Exact successor direction

The binary successor replaces `R` by `2R`. The positive take is

`g_p(2R) take g_p(R)`

`= 1/(p+R) take 1/(p+2R)`

`= R / [(p+R)(p+2R)]`.

Every factor is a generated positive carrier, so the take is positive for every forced sector and every finite generated support. Each formal Fold holding share therefore rises strictly at every depth successor.

This is the running direction on the native Fold support coordinate. It is not yet an identification of that coordinate with one universal laboratory-energy sign.

### Exact pair gap

For forced sectors `p<q`, the higher-index share exceeds the lower-index share by

`g_q(R) take g_p(R)`

`= 1/(p+R) take 1/(q+R)`

`= (q-p) / [(p+R)(q+R)]`.

The numerator is the fixed positive generator separation. Both denominator factors grow under the common support successor. Consequently

`gap_(p,q)(R) take gap_(p,q)(2R)`

is positive at every finite rung. All six ordered pairs in `(2,3,5,7)` pass the same identity and the same successor proof.

### Constructive convergence without a completed infinity

The claim does not invoke an already completed limit. It supplies a finite witness for every requested finite positive tolerance.

Let the requested tolerance be `1/N` and let `d = q-p`. Repeated binary generation reaches a finite support `R` satisfying

`R >= d N`.

Then

`gap_(p,q)(R) = d / [(p+R)(q+R)]`

is strictly smaller than `d/R^2`, and the chosen positive bound makes it strictly smaller than `1/N`. The implementation constructs the first binary support reaching the required bound and records the exact resulting gap.

This is depth-independent closure: the certificate works for every positive whole `N`, every forced ordered pair and every generated successor. The proof object is always finite and rational.

### Physical translation is carrier-specific

The common Fold coordinate is generated support. The independently sealed carrier laws determine how laboratory transfer scale maps onto that support:

- For the self-sourcing colour carrier, retained self-source support grows with interaction range. Larger physical momentum transfer probes shorter range, so measured `alpha_s` falls as energy rises.
- For electromagnetism, the relevant support is exposure after screening removal. Closer, higher-transfer probing removes screening layers, so measured electromagnetic `alpha` rises as energy rises.

The opposite laboratory-energy directions are therefore not an exception fitted after seeing the data. They are the separate translations already forced before this target opened. A candidate imposing one imported energy sign on all carriers was explicitly generated and rejected.

### What convergence means here

The theorem is exact on the shared Fold support axis: all four functions have shortfall `1/(p+R)` and every pair gap can be made smaller than any generated finite tolerance. It predicts common-support convergence of the complete forced sector ladder.

The current measurements establish scale dependence and the carrier-specific directions. They do not directly observe all four Fold sectors at arbitrary generated depth. The record therefore states the exact four-sector convergence as a standing falsifiable prediction. This is not an open mathematical derivation, but it is also not relabelled as an experiment that has already occurred.

## CHECK

### Formal checks

- The one-based supports reproduce `1,2,4,8,16,32` exactly.
- `g_2(4)=5/6` and its shortfall is `1/6`.
- The `(2,3)` gap at support four is `1/42`.
- Every sector successor take is positive through the explicit witness levels.
- Every one of the six pair gaps strictly shrinks through the explicit witness levels.
- Every pair passes finite tolerance denominators `1,2,3,5,7,11`; the unit test extends this independently to `17`.
- The base/successor proof establishes the same statements for arbitrary generated depth.
- Four hostile engine controls pass: false premise, changed source identity, duplicated census and boundary violation.

### Independent reconstruction

`generated/physics/coupling_running_convergence_terminal_validator_v1.py` imports neither the claimant nor the empirical adapter. It independently reconstructs:

- the 8,748 candidate identifiers;
- every sealed survival decision;
- the single surviving identifier;
- the binary support vector;
- every running share and exact pair-gap identity;
- every finite tolerance witness;
- closure scope and all four control classes.

Its implementation hash is `sha256:d6afafe211ebca3297f1e684cae091e39ca0cc80931b70c3cf11924360fdb9a1`, and its certificate hash is `sha256:dfa5fe2d5d8b63a7964d01f099c74abfb48ba490275147c4286b07f1b4645280`.

### Authoritative target custody

Only after the formal prediction was sealed did the validator release three registered target packages:

1. the official PDG 2026 QCD review;
2. the official PDG 2026 electroweak review;
3. the official PDG 2025 electroweak endpoint record retained for its explicitly printed Z-scale inverse coupling.

The source record is `experiments/external_sources/physics/snapshots/coupling-running-convergence-source-record.json`, frozen at `sha256:b83331089d96c073fbd5101753ba5c4716ae1a8b1b891e068684b6f7246d9953`.

The complete registered strong target contains:

- all seven rows and all reported columns of PDG Table 9.1;
- the exact `alpha_s(Q=1.37 GeV)=0.445 +/- 0.114` row;
- the exact `alpha_s(m_Z^2)=0.1180 +/- 0.0009` row;
- all nine measurement classes in Figure 9.4;
- the complete immutable Figure 9.4 rather than selected favorable points;
- the page-43 conclusion and the stated extension of measurements to 7 TeV.

PDG prints no numerical table of every Figure 9.4 point. The record preserves the entire vector graphic and does not fabricate precision by reverse-digitizing unprinted coordinates.

The exact separated intervals are

- low scale: `[331/1000, 559/1000]`;
- Z scale: `[1171/10000, 1189/10000]`.

The entire low-scale interval lies above the entire Z-scale interval. The strong coupling therefore decreases with increasing transfer energy across this exact comparison, consistent with the independently sealed self-source-range orientation.

The complete registered electromagnetic target contains:

- all five PDG 2026 inverse-alpha determinations on printed page 15;
- the adopted current interval around `137.035999177`;
- all eleven Table 10.1 hadronic `Delta alpha` evaluations and their uncertainties;
- the published relation `alpha(M_Z^2)=alpha(0)/[1-Delta alpha(M_Z^2)]`;
- the direct L3 and OPAL observations of electromagnetic running;
- the explicitly printed PDG 2025 Z-scale inverse-coupling row.

The explicit inverse-coupling intervals are

- Thomson limit: `[137035999063/1000000000, 27407199821/200000000]`;
- Z scale: `[63961/500, 63969/500]`.

The entire Thomson inverse interval lies above the entire Z-scale inverse interval. Therefore electromagnetic `alpha` increases toward the Z scale, consistent with the independently sealed screening-exposure orientation.

### Unfavorable controls

The empirical validator rejects each of the following:

- reversal of the observed strong interval order;
- omission of a QCD Table 9.1 row;
- omission of an electromagnetic Table 10.1 row;
- treating inverse alpha as if it increased toward the Z scale;
- forcing strong and electromagnetic couplings to use one physical energy sign;
- replacing the full multi-scale figure with an invented digitization;
- claiming that measured running is already a direct observation of unlimited four-sector convergence;
- opening any target before the prediction seal.

### Result classification

- Exact running functions: `closed`.
- Common binary scale transport: `closed`.
- Exact successor directions: `closed`.
- Exact pair-gap law: `closed`.
- Constructive depth-independent convergence: `closed`.
- Current authoritative strong and electromagnetic running comparisons: `passed`.
- Complete four-sector high-support convergence: `standing exact prediction`, not a fitted value and not a fabricated past measurement.

### Immutable receipts

- Derivation seal: `sha256:ba74e810bf7fdb5eaba6092188c295e809742ad65a33f8e4bc192f3a01326964`.
- Engine receipt: `sha256:b9263f585f01a2401261f465ad250f405a9914da5c53e57ba113f076aeb4f309`.
- Independent validation: `sha256:305e1659813dc637a2e779b3578008bc1144c3318872ad7f8d9a6c0ee6efe283`.
- Empirical validation: `sha256:2f1893b335cbcd3be69e125cc935f6b69ab16c56d65aedcc028cfe9c3e85d3cb`.
- Measurement receipt: `sha256:f7b8eb2dd10c0c689ee5dedb5b1add4a6b69ff8466d70e2727964d94289993bc`.
- Source manifest: `sha256:8e5b0d59dd3626f39dcb398cba57819bac31eca897583d65bb5b99084d67f3f9`.

The frozen engine computed and stored the survivor and admission. The claimant supplied neither an expected survivor identifier nor an admission flag.
