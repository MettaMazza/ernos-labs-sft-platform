# Why ELEC-008 requires a derivation check

Band alignment retains both positive levels, exact offset magnitude, held ordering and interface identity without signed proof magnitudes.

The 256-form grammar is generated before target release, independently reconstructed and compared against every registered authoritative record.
