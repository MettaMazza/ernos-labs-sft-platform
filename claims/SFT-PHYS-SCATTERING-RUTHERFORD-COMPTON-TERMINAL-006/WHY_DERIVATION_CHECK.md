# SFT-PHYS-SCATTERING-RUTHERFORD-COMPTON-TERMINAL-006: WHY / DERIVATION / CHECK

## WHY

The predecessor scattering receipt defines a scattering experiment as a complete map from prepared incoming words to distinguishable outgoing channels and defines cross section as exact observed channel support relative to incident support and geometry. That is the necessary foundation, but V1 VI-1 asks for the laws that make scattering quantitatively distinctive:

1. what a Fold amplitude means without importing a complex-number postulate;
2. why an observed channel carries the paired or “squared” weight associated with the Born structure;
3. why Coulomb scattering has the inverse-fourth-power half-angle form rather than merely “more forward scattering”;
4. why Compton wavelength shift grows with deflection and why photon energy must decrease; and
5. what exact dimensional values are obtained when the sealed relations are opened against authoritative measurements.

This successor closes those questions in one compositional law. It does not insert Rutherford’s or Compton’s formula into the candidate selector. The formal program sees only positive exact direction-transfer parts, held phase and carrier labels, structural absence, closed energy transfer and previously admitted Fold laws. The IAEA equations, historical observation and NIST constants remain inaccessible until the prediction is sealed.

## DERIVATION

### Dependency route to the One

The engine registration traces the result through admitted claims to the foundational theorem:

- `SFT-PHYS-MATTER-SCATTERING-001` supplies the complete incoming-to-outgoing channel map and source-normalized cross-section measure.
- `SFT-PHYS-QUANTUM-PHYSICAL-STATE-001` supplies complete phase-labelled physical support.
- `SFT-PHYS-QUANTUM-WEIGHT-001` supplies exact compatible-predecessor support ratios.
- `SFT-PHYS-MECH-CONSERVATION-001` supplies closed energy and momentum-carrier transfer.
- `SFT-PHYS-FIELD-INVERSE-SQUARE-001` and `SFT-PHYS-FIELD-COULOMB-GAUSS-CLOSURE-003` supply the rank-two conserved Coulomb source.
- `SFT-PHYS-FIELD-ELECTRIC-POTENTIAL-001` supplies work and potential transfer per held charge.
- `SFT-PHYS-MATTER-MASS-ENERGY-001` supplies electron inertial/rest-energy correspondence.
- `SFT-PHYS-SPACETIME-LIMIT-SPEED-001` and `SFT-PHYS-WAVE-SPEED-LENGTH-FREQUENCY-001` supply the limiting-speed wavelength carrier.
- dimensional composition, dimensional consistency, exact arithmetic, combinatorics, measured-value boundary, uncertainty and target custody govern the symbolic and empirical translations.

The formal claimant contains no source path, particle name, measured angle, measured cross section, wavelength, energy or target value. It contains no stored expected survivor and no self-awarded admission flag. The untouched engine computes and records the survivor and admission result.

### Exact direction-transfer coordinate

Let `u` be the exact positive part of the complete direction change carried by a nonforward outgoing channel, with

`0 < u <= One`

written operationally as a positive rational part rather than by introducing a numerical-zero proof value. The unchanged forward channel carries no direction-transfer act and is therefore the structural empty form `()`.

At the external comparison boundary, the ordinary angular coordinate is related by

`u = sin^2(theta/2) = (1 - cos(theta))/2`.

The Fold proof does not evaluate an irrational sine or cosine. It derives relations over exact `u`; the conventional angular expression is a reversible label translation after sealing.

### Amplitude without an imported complex magnitude

A Fold amplitude carrier is one exact phase-compatible overlap leg: the support that connects preparation to the generated transfer boundary, or that boundary to the retained outgoing record. An observed differential channel requires both legs. Its full compatible predecessor fibre is therefore the exact pair

`A(u) paired with A(u)`.

For the Coulomb transfer boundary, one conserved leg is distributed over the exact transfer part, so its normalized support is `A(u)=1/u`. Pairing the two required legs gives

`W(u) = 1/(u*u)`.

No signed or imaginary proof magnitude is needed. Held phase labels are still retained—so interference is not erased—but this channel’s positive differential support is the exact self-composition of two phase-compatible legs. The earlier branch-count law remains intact: the complete paired predecessor fibre, not either leg in isolation, is what the observation counts.

### Finite annulus proof of the Coulomb exponent

The inverse-square result is not admitted merely by naming the desired point law. The exact finite geometry supplies a second certificate.

For transfer part `u`, the complete direction is partitioned into the turned hand `u` and its retained complement `One take u`. The incident impact-area support above that transfer boundary is

`C(u) = (One take u)/u`.

At complete backscatter `u=One`, the complement is `()`, not numerical zero. For any exact ordered pair `a<b`, the annular incident area removed between the two direction classes divided by their transfer interval is

`[C(a) take C(b)]/[b take a] = 1/(a*b)`.

This identity is established by exact positive subtraction at every rational boundary. As the generated bracket closes on a named rational part `u`, both factors become `u`, forcing `1/u^2`. The proof therefore has a finite exact certificate at every bracket and a depth-independent point closure; it does not rely on a floating derivative or an infinite numerical object.

For the registered exact parts, the complete normalized vector is:

| `u` | `1/u^2` |
|---:|---:|
| `1/4` | `16` |
| `1/2` | `4` |
| `3/4` | `16/9` |
| `1` | `1` |

Under `u=sin^2(theta/2)`, this is exactly `sin^-4(theta/2)`.

### Coulomb charge and energy scale

Let `Q` be the complete held product of projectile and target charge carriers, and `E` the positive incident kinetic-energy carrier. Closed Coulomb approach fixes the backscatter length support as proportional to `Q/E`. A differential area pairs that length support with itself. With the generated four-part boundary convention, the exact scale is

`K = [Q/(4E)] paired with [Q/(4E)]`.

The differential support is therefore

`Sigma(u) = K/u^2`.

Two exact adverse scalings distinguish this from linear or fitted alternatives:

- doubling the charge product multiplies differential support by `4`;
- doubling incident energy multiplies it by `1/4`.

No target-derived coefficient chooses either exponent. Dimensional charge and energy values may instantiate the sealed law later, but cannot alter its form.

### Compton transfer from conservation

Let `x=E/M` be the incident photon energy relative to the held electron rest-energy support, and let `r=E'/E` be the outgoing-to-incident photon-energy ratio. For a nonforward channel, closed energy-direction transfer makes the inverse outgoing photon support exceed the inverse incoming support. Exact positive take yields

`[1/(x*r) take 1/x] = 2u`.

Solving this exact relation gives one positive form:

`r = 1/(1 + 2*x*u)`.

Every nonforward `u` therefore makes `r` a proper part of the One: the photon loses energy to the named recoil-electron carrier. An elastic nonforward photon and an energy-gaining outgoing photon both fail the conservation identity.

Photon wavelength is action-times-speed over photon energy. The electron Compton carrier is action over electron inertia and limiting speed,

`lambda_e = h/(m_e*c)`.

Multiplying the inverse-energy change by the electron rest carrier gives the wavelength increase

`Delta lambda/lambda_e = 2u`.

Thus

`Delta lambda = 2u*lambda_e`.

At the external angular translation `2u=1-cos(theta)`, this is the Compton shift relation. The exact registered vector is:

| `u` | shift in `lambda_e` carriers | `E'/E` when `E=M` |
|---:|---:|---:|
| `1/4` | `1/2` | `2/3` |
| `1/2` | `1` | `1/2` |
| `3/4` | `3/2` | `2/5` |
| `1` | `2` | `1/3` |

### High-energy ceilings without an infinity object

For generated positive incident-energy depth `n`, outgoing photon energy in electron-rest units is

`F_n(u) = n/(1+2*n*u)`.

The successor step is exactly positive:

`F_(n+1)(u) take F_n(u) = 1/[(1+2*u*n)*(1+2*u*(n+1))]`.

The gap to the unique ceiling is also exactly positive:

`1/(2u) take F_n(u) = 1/[2*u*(1+2*u*n)]`.

No infinite value is formed. Exact successor induction proves that every generated value increases while remaining below `1/(2u)`. Consequently:

- at a right angle, `u=1/2`, the ceiling is `One` electron rest-energy support;
- at backscatter, `u=1`, the ceiling is `half-One` electron rest-energy support.

### Complete candidate grammar

Eight axes are generated:

| Axis | Alternatives |
|---|---:|
| Amplitude carrier | 3 |
| Cross-section measure | 3 |
| Coulomb angular power | 3 |
| Coulomb charge/energy scale | 3 |
| Photon wavelength-transfer law | 3 |
| Photon energy-transfer law | 3 |
| Target custody | 2 |
| Extension | 2 |

The complete Cartesian product contains

`3^6 * 2^2 = 2,916`

forms. Every form is generated exactly once and receives one computed decision. The alternatives explicitly include single-leg counting, first-power or angle-independent Coulomb response, linear charge/energy scaling, one-part or angle-independent wavelength change, elastic or energy-gaining photon response, exposed target access and free correction.

Exactly one assembled form survives:

`paired-phase-compatible-overlap-legs__paired-weight-per-incident-boundary-support__inverse-transfer-part-squared__charge-product-squared-energy-inverse-squared__two-transfer-parts-times-action-over-inertia-speed__rest-over-rest-plus-two-energy-transfer-parts__sealed-before-release__empty-extension`

The survivor identity above is an engine output, not a claimant input.

## CHECK

### Implementation-distinct reconstruction

The independent validator imports neither the claimant nor any empirical adapter. It separately reconstructs:

- all 2,916 candidate identities and decisions;
- cumulative impact-area and finite-annulus identities;
- the `16, 4, 16/9, 1` Coulomb vector;
- charge and energy scaling;
- the `1/2, 1, 3/2, 2` Compton shift vector;
- the exact outgoing-energy relation and high-energy ceilings;
- structural forward absence; and
- all four mandatory hostile controls.

It computes one survivor. Its implementation hash is `sha256:0d35b75c4957b1b77081f92b1cfcf6cfda6596302a2ae3b324a7242b1a03accb`; its certificate hash is `sha256:633aaa0ff4b45f924cfe3a7ef076b2ddac8083b464eada0999874de472b80e99`.

### Target-inaccessible prediction

The capability-closed Fold interpreter received the sealed exact vectors and scope labels, but no IAEA page, formula, historical count, CODATA row or comparator. Filesystem, network, subprocess, environment, clock, dynamic import and foreign-function access were denied. The prediction sealed at `sha256:cdfe1b77377ea1c4d889a71c84ea6fc497c03b551ee299142f5cabedd789e935` before the target vault released the two-source record.

The protocol is observational-data-informed but target inaccessible: prior V1/V2 work identifies what must be reconstructed, while the measurements and conventional formulas cannot select a candidate or enter the derivation trace.

### IAEA Rutherford comparison

The complete registered IAEA pages retain:

- the Geiger-Marsden observation that approximately one alpha particle in `10^4` was scattered beyond 90 degrees, contrasted with the obsolete diffuse-charge prediction order `10^-3500`;
- repulsive alpha-nucleus Coulomb force with inverse-square radial form;
- closest-approach scale proportional to charge product over incident kinetic energy; and
- differential Rutherford cross section proportional to `sin^-4(theta/2)` with the scale paired as an area.

The external negative exponent belongs only to the historical source inscription and never enters the positive formal proof domain. The observed nonempty large-angle record rejects an erased-backscatter model. The exact IAEA angular dependence matches every sealed rational comparison point under `u=sin^2(theta/2)`.

The handbook’s scope is retained: the Rutherford relation is for repulsive Coulomb scattering of the charged projectile by a more massive charged nucleus under its stated nonrelativistic point-boundary conditions. Nuclear, screening, finite-size or relativistic departures outside that boundary are not silently relabelled as failures of the scoped law.

### IAEA Compton comparison

The registered IAEA page retains the free, initially stationary electron boundary, the requirement that incident photon energy exceed binding support, closed energy and momentum transfer, the wavelength shift

`Delta lambda = lambda_e*(1-cos(theta))`,

and the carrier definition

`lambda_e=h/(m_e*c)`.

Under `1-cos(theta)=2u`, the source relation is identical to the sealed `2u` vector. The right-angle and backscatter ceilings are algebraic consequences of this retained IAEA transfer relation evaluated with the independently released NIST electron rest-energy interval; they are not presented as a separately fitted IAEA scalar.

### Exact NIST CODATA measured-value comparison

All seven registered rows are retained with their reported units and uncertainties:

1. Planck constant;
2. speed of light in vacuum;
3. electron mass;
4. electron Compton wavelength;
5. electron mass-energy equivalent in MeV;
6. classical electron radius; and
7. Thomson cross section.

The first two are exact in the registered CODATA table. Exact interval propagation of `h/(m_e*c)` gives

`[6310543/2600880509804446332, 18931629/7802641524616659668] m`,

or approximately `2.42631023463453e-12` to `2.42631023612611e-12 m` only when rendered for reading.

The independently reported NIST Compton-wavelength interval is

`[121315511731/50000000000000000000000, 121315511807/50000000000000000000000] m`,

or `2.42631023462e-12` to `2.42631023614e-12 m`. The complete intervals overlap. The IAEA rounded `0.024 angstrom` inscription contains the NIST interval at its stated precision.

The NIST electron rest-energy interval is

`[51099895053/100000000000, 10219979017/20000000000] MeV`,

or `0.51099895053–0.51099895085 MeV`. Therefore the sealed right-angle ceiling carries that same interval, while the backscatter ceiling is exactly half:

`[51099895053/200000000000, 10219979017/40000000000] MeV`,

or `0.255499475265–0.255499475425 MeV`.

No measured value selects the two or the half. Those relations were sealed first; NIST supplies the dimensional realization afterward.

### Adverse controls

- A single overlap leg fails the paired observed-channel support.
- First-power and angle-independent Coulomb vectors fail both the point and finite-annulus certificates.
- Linear charge/energy scaling fails the exact doubling controls.
- One-transfer-part and angle-independent Compton shifts fail closed conservation.
- Elastic and energy-gaining nonforward photons fail the exact inverse-energy transfer identity.
- Replacing forward structural absence with a numerical proof value fails the boundary control.
- Removing one CODATA row fails complete target retention.
- Changing either source byte fails its registered hash.
- Pre-seal target access and free correction both fail the generated grammar.

### Receipt and falsification

The untouched repository coordinator admitted the claim with all nine gates passing, and the same receipt was reproduced for evidence materialization:

- engine receipt: `sha256:2c2e6212e5bb27a2b7647e8eca1583bc75775c886f2185885a7925d83e195b91`;
- derivation seal: `sha256:dfe407aa03c635803c44c57974d102983d3873a525baa241ca3cc4c31e913180`;
- empirical validation: `sha256:4bd3b98fb21c2f4758f64c4b2bba3df272ab97ea863c67477988b192679cf56e`;
- measurement receipt: `sha256:d6c7057f63d6f29411d7fbddef78e3a8cd96aeeadcf8f8ef1348b0c395c17cd0`.

Within its declared boundary, the claim is falsified if the complete IAEA record ceases to retain the inverse-square Coulomb source, inverse-fourth-power half-angle law, nonempty large-angle support, closed Compton transfer or wavelength-shift relation; if exact `h/(m_e*c)` does not overlap the NIST Compton-wavelength interval; if the derived right-angle/backscatter ceilings disagree with the NIST rest-energy vector under the retained IAEA transfer relation; if a registered row, uncertainty, assumption, limitation or hostile control is omitted; if source identity changes; or if target material enters candidate selection before sealing.
