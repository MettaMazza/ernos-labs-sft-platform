# Fold-P equals Fold-NP law

Claim: `SFT-COMP-CPLX-FOLD-P-NP-EQUALITY-002`

## WHY

V2 Step 405 closes a native equality that the archived inventory explicitly left outside scope; equal-strength reconstruction requires the two containments and their boundary.

## DERIVATION

The previously forced evaluator and proof system act on the same generated word and the same unique suffix edges. Evaluation emits the proof, and proof soundness admits no alternative result; their exact depth resources coincide.

Boundary:

> Every generated positive finite native Fold word, its unique suffix-closing evaluation and certificates composed only from the registered trace proof system.

The complete grammar contains 256 candidates across eight binary axes. Exactly one retains every requirement.

- `domain` -> `complete-native-fold-word-family`: Every generated native word is included.
- `evaluator` -> `unique-exact-closing-trace`: Evaluation retains every forced edge.
- `certificate` -> `compiled-lawful-evaluation-trace`: The certificate is exactly the source-bound transition trace.
- `verifier` -> `edgewise-sound-complete-verifier`: The verifier checks source, every forced edge, terminal and length.
- `p_to_np` -> `evaluator-emits-accepted-certificate`: Every exact evaluation supplies an accepted trace.
- `np_to_p` -> `soundness-forces-unique-evaluation`: Every accepted trace equals the deterministic suffix run.
- `successor` -> `prepend-label-trace-successor`: One new label adds one evaluator and verifier edge.
- `boundary` -> `native-equality-only`: The equality remains exactly inside the admitted Fold grammar.

Forced result:

> The sole all-preserving kernel forces P_F=NP_F with exact resource k for every supplied positive finite native description depth, without asserting conventional P=NP.

Operational laws:

- every deterministic evaluation emits its accepted exact trace
- every accepted exact trace equals the unique native evaluation
- evaluation and verification each traverse one edge per word position
- the two exact containments force native equality

Base:

> At the first positive depth, the single edge is both the evaluation and its complete verified certificate.

Successor:

> Prepending either native label adds exactly one forced evaluation edge and one verifier check while preserving source, suffix and terminal equality.

## CHECK

Execute every word through depth seven, verify each compiled trace, reject a changed trace, execute the successor through depth fourteen, exhaust all 256 structural candidates and independently regenerate the census.

- `complete-depth-seven`: Every native source through depth seven has one accepted exact evaluation certificate.
- `resource-equality`: Evaluation and verification lengths equal source depth through fourteen.
- `tamper-rejection`: A changed first certificate state rejects for every depth through seven.

The false-premise, changed-source, changed-survivor and excluded-boundary controls must all reject. The independent validator regenerates the literal product without importing this scientific module.

## Exact limitation

This equality is confined to the admitted Fold word/process grammar. It does not decide conventional P versus NP for arbitrary languages, machines, encodings or Boolean gate bases.

- no arbitrary external language family
- no nondeterministic machine import
- no polynomial convention import
- no conventional P-versus-NP conclusion
- no hidden certificate advice
