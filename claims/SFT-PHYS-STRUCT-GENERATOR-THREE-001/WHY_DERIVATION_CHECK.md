# The second structural Fold generator is three

Claim: `SFT-PHYS-STRUCT-GENERATOR-THREE-001`

## Why

The clean-room V3 chain requires the generator-three result before spatial
dimension or inverse-square dilution can lawfully depend on it. The question is
reconstructed from V2, but no V2 proof artifact or physical target enters the
derivation.

## Derivation

Complete Fold-word support at positive depth `p` contains the recursively
doubled support. Its unique positive predecessor supplies the denominator of a
unit part. At depth two this constructs one-third, whose complete Fold orbit has
first-return count two. Positive-count discreteness makes three the least count
distinct from and after two. The depth-three construction supplies one-seventh,
whose retained orbit is exactly one-seventh, two-sevenths, four-sevenths and
back to one-seventh. Existence and minimal order therefore agree on three.

The ten binary axes generate 1,024 forms and leave one survivor. Measurement is
explicitly downstream.

## Check

The primary execution and an implementation-distinct validator regenerate the
complete candidate product and both first-return orbits. False-premise,
tampered-source, tampered-artifact and forbidden-boundary controls must pass.
