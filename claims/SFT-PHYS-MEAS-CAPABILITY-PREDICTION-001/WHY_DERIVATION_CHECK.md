# Capability-closed exact Fold prediction form

Claim: `SFT-PHYS-MEAS-CAPABILITY-PREDICTION-001`

## Exact statement

Within the registered finite prediction grammar, a physical prediction can be target-inaccessible, exact, portable and completely traced in exactly one form: immutable registered Fold inputs pass through a data-only sequence of generated exact operations to one terminal emission, while every ambient host capability and unregistered operation is absent and any violation halts.

## Generated grammar and uniqueness

All finite data-only prediction interpreters assembled from admitted exact values, registered inputs, generated operations, immutable held states, a declared terminal emission and no host capability.

The registered product exhausts every combination of the eight declared binary
axes, giving exactly 256 named forms. A form survives only when every axis
retains target inaccessibility, exact provenance, complete evidence and
fail-closed authority. Therefore the sole survivor is:

`registered-input-only__exact-fold-domain__data-only-generated-operations__no-ambient-capability__single-assignment-held-state__one-terminal-emission__complete-step-trace__no-extra-rule`

- `input`: `registered-input-only` is the only preserving choice; `ambient-input` — Ambient input can contain the withheld target or an answer-producing channel.; `registered-input-only` — Every readable input is named and hashed before execution.
- `value`: `exact-fold-domain` is the only preserving choice; `host-scalar-domain` — Host scalars import forbidden or platform-dependent proof values.; `exact-fold-domain` — Structural Empty One, positive counts, exact ratios, held labels and finite products retain exact provenance.
- `operation`: `data-only-generated-operations` is the only preserving choice; `contributor-executable-source` — Arbitrary source can create operations outside the registered grammar.; `data-only-generated-operations` — Every instruction is selected from a closed, registered operation set.
- `capability`: `no-ambient-capability` is the only preserving choice; `ambient-capability` — An ambient resource can reveal target, comparison or mutable authority state.; `no-ambient-capability` — No registered instruction denotes filesystem, network, process, clock, environment, dynamic import or foreign function access.
- `state`: `single-assignment-held-state` is the only preserving choice; `mutable-overwrite` — Silent overwrite loses the exact predecessor and permits untraced replacement.; `single-assignment-held-state` — Each register is held once and every read names a generated predecessor.
- `termination`: `one-terminal-emission` is the only preserving choice; `implicit-or-open-ended` — Implicit or open-ended completion cannot certify a complete prediction artifact.; `one-terminal-emission` — Exactly one final emission closes the finite registered program.
- `trace`: `complete-step-trace` is the only preserving choice; `result-only` — A result alone hides the operational relation and its resource path.; `complete-step-trace` — Every instruction position, arguments and output receive exact identities.
- `extension`: `no-extra-rule` is the only preserving choice; `extra-rule` — An extra instruction or capability is a free answer-selecting parameter.; `no-extra-rule` — The runtime contains only the generated preserving surface.

No axis is a fitted parameter: both alternatives are generated before
execution and the dependencies reject the non-preserving alternative.

## Operational witnesses

- `closed-surface`: The executable opcode enumeration contains no ambient host operation.
- `exact-round-trip`: A registered positive Fold count passes through input and emission with an exact complete trace.

The One base is: One registered instruction consumes one declared exact value and emits one fully identified exact value.

The successor certificate is: Appending one registered instruction reads only previously held exact values, writes one fresh held value and appends one identified trace row without adding a capability.

## Boundary

- arbitrary Python or native contributor code
- filesystem, network, subprocess, clock, environment, import and foreign-function access
- floating, negative, irrational, imaginary and semantic-zero proof values
- unbounded or untraced execution

This is a formal prerequisite for empirical work. It is not itself a claim that
nature follows any physical relation. Four adverse controls and a separate
standard-library validator regenerate the full product and its sole survivor.
