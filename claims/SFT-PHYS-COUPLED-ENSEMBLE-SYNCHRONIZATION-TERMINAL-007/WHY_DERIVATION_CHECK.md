# Why this is a derivation check

The formal claimant does not know the V1 ensemble or its answer. It enumerates 2,916 combinations across coupling domain, paired action, balance, residual semantics, ensemble terminal, closure, target custody and extension. Exactly one computed form preserves every carrier.

For any positive ordered pair with separation `s`, equal coupled movement gives each member `g*s`. Exact meeting requires the two moves to reassemble `s`, hence `g*s + g*s = s`. Complete binary support uniquely forces `g = 1/2`. At half-One the residual is structural emptiness; below or above it the residual multiplier is the positive difference between the One and twice the coupling. Once every ensemble member is equal, a common Fold successor maps them to the same image and synchronization is preserved at every depth.

The law deliberately does not claim that every arbitrary nearest-neighbour ensemble must converge. That stronger statement is neither needed nor imported. The specific V1 E4 ensemble is a post-seal empirical reconstruction: an independent exact implementation retains all five coupling runs, all 75 region-count coordinates and every final point. It reproduces `(7,7,1,5,5)`, with the half-One recurrence `(12,10,7,5,4,3,2,1,1,1,1,1,1,1,1)` and all twenty terminal members at `4/7`. The unfavorable coupling rows remain visible.

The untouched engine computed the unique survivor and admitted it only after all nine gates passed.
