# Exact insertion and elimination pathways

## WHY

INORG-013 requires the complete adjacency transformation, including scope and absence evidence, rather than a named mechanism or selected product formula.

## DERIVATION

Insertion replaces `X–Z` with `X–Y` and `Y–Z` while conserving all carriers. Extrusion is its exact inverse. Migratory insertion composes migration and insertion, replacing metal–migrant adjacency by migrant–inserted adjacency while retaining metal–inserted adjacency. Elimination retains both eliminands and centres and generates the exact product adjacency with a held unsaturation, ring or carbene boundary.

## CHECK

The 256-form grammar has one depth-independent survivor and an implementation-distinct adjacency-difference reconstruction. Ten frozen IUPAC rows preserve all three records, the host-lattice scope distinction, and the introduced but absent rendered carbene-example structure. A focused pre-admission test rejected the first target-analysis boundary because it read the following sentence as example content; that predecessor remains preserved, and the versioned correction isolates only the text between the example colon and the next sentence without changing identities, source bytes, law, grammar or comparison rows.
