# Exact finite graph and network structure

Claim: `SFT-MATH-GRAPH-NETWORK-001`

## Why

Graphs are the minimal mathematics of explicit relations and paths. Their laws must follow from generated carriers and pair support rather than imported matrices, weights or probabilistic ensembles.

## Derivation

Discrete mathematics supplies canonical carriers and relations; combinatorics supplies complete path-family generation. Exhausting nine structural choices leaves the relation/path/return/pairing graph kernel.

The registered grammar boundary is:

> All finite graphs and networks generated from canonical finite form carriers, held pair-cell relations, complete path traces, return relations and exact positive flow-token pairings.

The complete product has `512` candidates across
`9` declared structural dimensions. Every dimension has one
all-preserving coordinate and at least one explicit failure coordinate. Exactly
one product member combines all preserving coordinates.

The admitted result is:

> The graph/network kernel is complete canonical nodes with held ordered pair-cell edges, complete adjacent paths, witnessed reachability and return, held crossing cuts, ingress/egress pairing and no extra rule.

Its operational laws are:

- path concatenation is lawful exactly when the interface node agrees.
- reachability is reflexive by the retained identity path and transitive by path composition.
- a tree is a connected generated graph with no nontrivial return path.
- a cut contains every and only relation crossing a held/complementary node partition.
- network balance preserves token identity rather than cancelling signed magnitudes.

Depth-independent closure uses this base:

> One canonical node supplies the identity path and contains no nontrivial edge cycle.

and this successor certificate:

> Appending one fresh node generates exactly its ordered pair cells with prior nodes; any held edge selection extends paths locally, and connectivity, cycle, cut and balance witnesses update only through those new cells.

The exact exclusions are:

- no ungenerated vertex or edge.
- no negative edge or flow quantity.
- no stochastic graph law.
- no infinite graph as a completed object.

## Check

Execute all 512 graph kernels, validate path and missing-edge controls, reachability, cycle return, cut selection and paired network balance, then independently regenerate the census.

Operational witnesses:

- `valid-path`: The chain retains both required adjacent edges.
- `missing-edge-control`: An endpoint sequence with a missing adjacency is not a path.
- `reachability`: Generated path search reaches c from a and does not reach a from c in the directed chain.
- `cycle-return`: The explicit c-to-a return distinguishes the cycle from the chain.
- `cut-selection`: Holding the first chain node selects exactly its crossing edge.
- `paired-balance`: Equal unique ingress and egress token traces witness internal balance.

Four required controls reject a false premise, changed source identity, altered
survivor set and excluded boundary import. The separate validator independently
regenerates the literal candidate product and sole survivor without importing the
scientific law module.

## Boundary

This law closes exact generated finite graph structure. Weighted geometry, random graphs, infinite graph limits and algorithmic graph complexity require later derived laws.

Conventional correspondence terms, admitted only after the derivation is sealed:
directed graph, path, reachability, cycle, tree, cut, network flow.
