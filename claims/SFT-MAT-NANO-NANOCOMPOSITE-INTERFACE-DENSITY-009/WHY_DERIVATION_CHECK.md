# Why NANO-009 requires a derivation check

A nanocomposite retains matrix units, inclusion units, their distinct identities and every counted interfacial contact, yielding an exact contact-per-unit fraction without fitted constitutive parameters.

The 256-form grammar is generated before target release, independently reconstructed and compared against every registered authoritative record.
