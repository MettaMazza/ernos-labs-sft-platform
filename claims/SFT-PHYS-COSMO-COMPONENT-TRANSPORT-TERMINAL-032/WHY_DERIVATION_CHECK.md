# Why / Derivation / Check

## Why this result matters

The earlier corpus correctly identified the three cosmic transport classes,
their density ordering, a matter-vacuum expansion curve and two transition
conditions.  It also used the then-leading `1/3` matter and `2/3` vacuum split.
V3 has since forced and empirically admitted the stronger terminal budget:
matter `5/16`, vacuum `11/16`.  Reusing the earlier curve or its `1/2`
deceleration magnitude would therefore be inconsistent.  This claim performs
the required successor derivation from the terminal budget and preserves the
old rows as adverse evidence rather than silently rewriting them.

The reconstruction also enforces the SFT quantity language.  Conventional
notations such as `a^-3`, `w=-1` and `q<0` are not admitted proof scalars.
Forward dilution is represented by exact reciprocal transport; the proof uses
positive past stretch powers.  Vacuum tension and accelerated expansion are
held orientations paired with positive exact magnitudes.  Conventional signed
notation appears only in the source-bound external correspondence record.

## Derivation

Let `r` be a positive exact Fold stretch from the present observation carrier
to an earlier carrier.  The independently admitted stable spatial generator
has three directions.  Fixed matter content is distributed over the complete
three-direction volume, so its earlier density carrier is multiplied by

`r^3`.

Radiation occupies the same volume and its recurrence carrier acquires one
additional stretch.  Its earlier density carrier is therefore multiplied by

`r^4`.

The vacuum carrier is Fold invariant.  Its transport factor remains the One at
every successor.  In conventional language these three relations correspond
to matter dilution `a^-3`, radiation dilution `a^-4`, and invariant vacuum
`a^0`.  Inside SFT the empty exponent is a typed absence of dilution, not a
numerical zero exponent.

The admitted present late-time shares are

`M = 5/16`, `V = 11/16`, and `M+V = One`.

Exact composition forces the dimensionless squared expansion carrier

`E2(r) = V + M r^3 = (11 + 5r^3)/16`.

No expansion equation or measured Hubble value selects this relation.  A
dimensional measured `H` is formed only after sealing by transporting the
separately held measured `H0` interval through `H^2 = H0^2 E2(r)`.

The epoch fractions are therefore

`Omega_m(r) = 5r^3/(11+5r^3)`

and

`Omega_v(r) = 11/(11+5r^3)`.

They reassemble the One at every positive exact stretch.  At `r=One`, the
matter fraction is `5/16`.  At `r=2` it is `40/51`; at `r=3` it is `135/146`.
Because radiation has power four, matter power three and vacuum invariant
power, the backward ordering is forced: radiation overtakes matter, and matter
overtakes vacuum.  The corresponding forward ordering is radiation, matter,
then vacuum dominance.

Matter-vacuum equality is obtained without a cube root:

`5r^3 = 11`, hence `r^3 = 11/5`.

The decelerating matter carrier has half the matter share while the vacuum
tension carrier has the complete vacuum share.  Their equality gives

`5r^3/32 = 11/16`, hence `r^3 = 22/5`.

At present stretch the vacuum carrier exceeds the matter-half carrier by

`11/16 - 5/32 = 17/32`.

The result is a positive magnitude held with the `accelerating` orientation.
The conventional external notation is a negative deceleration parameter, but
no negative number enters the derivation.

Vacuum invariance likewise forces tension with magnitude the One.  The
conventional correspondence label is `w=-1`; the SFT theorem contains the
positive One magnitude and the held `tension` orientation.

## Exhaustive alternative closure

Twelve binary axes generate exactly `2^12 = 4,096` candidates.  They test:

1. borrowed three-dimensional volume against the admitted generator-three
   carrier;
2. a selected matter exponent against inverse-volume third-power transport;
3. a selected radiation exponent against volume plus recurrence transport;
4. a named static density against the Fold-invariant One;
5. the superseded two-thirds curve against the terminal eleven-five curve;
6. fitted epoch densities against exact component-over-whole fractions;
7. a selected decimal cube root against the exact `11/5` cube condition;
8. the old `1/2` and `4` results against `17/32` and `22/5`;
9. negative proof scalars against positive magnitude plus held orientation;
10. a fitted Hubble normalization against post-seal reference transport;
11. target-readable construction against capability-closed custody; and
12. an inserted correction against no additional rule.

Every candidate is generated and decided.  Exactly one preserves every
requirement.  The successor certificate is depth-independent because adding a
positive exact stretch preserves the third, fourth and invariant transport
classes and their exact component closure.

## Complete external measurement check

The structural survivor was sealed before the empirical evaluator opened its
measurement records.  The evaluator retained five independent source groups:

- the complete 32-row cosmic-chronometer `H(z)` table;
- the Planck-plus-BAO `H0`, matter and vacuum intervals;
- the model-reduced current-acceleration magnitude and transition interval;
- the combined constant-vacuum equation-of-state interval; and
- the DESI DR2 preference for the `w0-wa` evolving-dark-energy extension.

For every chronometer row the evaluator avoids an irrational square root.  It
forms the exact positive predicted interval

`[H0_lower^2 E2(r), H0_upper^2 E2(r)]`

and compares it with the square of the complete reported two-uncertainty
interval.  When a conventional uncertainty interval reaches below physical
emptiness, its lower endpoint is the typed empty measurement boundary.  All 32
rows overlap.  Four rows lie outside their one-standard-uncertainty boundary;
they are retained and pass at the preregistered two-uncertainty boundary.  No
row was dropped and no tolerance was optimized after comparison.

The exact `11/5` equality cube lies inside the complete outward-propagated
Planck vacuum-to-matter ratio interval.  The `22/5` acceleration cube lies
inside both twice that ratio interval and the registered measured transition
interval after exact cubing.  The `17/32` accelerating magnitude lies inside
the registered current-acceleration magnitude interval.  Vacuum tension of One
magnitude lies inside the reported 68-percent constant-`w` magnitude interval.

DESI DR2 reports that the `w0-wa` extension improves the joint-data fit at a
dataset-dependent significance.  That adverse row is retained exactly.  It is
not erased, downgraded to a favorable result, or allowed to select a different
structural law.  It remains a direct discriminator between the sealed static
vacuum law and an evolving state interpretation as further measurements
accumulate.

## Machine evidence

- Candidate count: `4,096`.
- Empirical target rows: `37`, plus one deliberately tampered control.
- Closure: `depth_independent`.
- Source manifest:
  `sha256:e1004c307761e6e384dfebeb1cacc43157ac49b91d746bfe9cef867d59fadc1d`.
- Derivation seal:
  `sha256:8efa797d1e574e5c87696c84bbdd034a73bc59b71b68d3f60850c8c00172f2e0`.
- Independent validation:
  `sha256:1912e7d1290e48f17710a1214bb1b33ff3979ce747d8b85f9b9194589ec3db8f`.
- Empirical validation:
  `sha256:2356d7946091e0bf3fc556d6750ecf94f76435c1dedb3ef864edc81d59b87a9e`.
- Measurement receipt:
  `sha256:a33e0831e949329261b8cf0d72892053904c03dc38d4e21f086127b16196697a`.
- Engine receipt:
  `sha256:504257d441fad45a8f173fad45015c7d723caa7636c517e71d27fddc43a2c42d`.

This closes the declared terminal component-transport family against the
present registered evidence while leaving lawful future observations free to
falsify, extend or supersede it through the same untouched engine.
