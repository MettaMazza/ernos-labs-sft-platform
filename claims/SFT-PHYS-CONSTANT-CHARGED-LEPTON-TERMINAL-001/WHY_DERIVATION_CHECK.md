# Terminal self-coupling refinement of the charged-lepton cubic

This claim responds to the preserved failure of the first exact CODATA test.
It is therefore disclosed as an `observational_derivation`, not misrepresented
as a blind forward discovery.

The executable relation itself receives no measured lepton value. It retains
the admitted `3/1454` product, applies one terminal alpha coupling to each of
the three roots, distributes that action across the complete `3^3` carrier,
and weights the retained correction by the independently forced down/up cover
depths `5` and `7`; the up-depth crosses one further alpha order and one of the
three channels. Thus the exact held correction is

`alpha^3 / 3^3 * (5 + (7/3) alpha)`.

The complete ten-axis grammar contains 1,024 forms. Exactly one retains the
admitted base, root order, complete support, typed depth roles, held
orientation, target isolation, disclosed provenance and no-extra-rule law.

Admission is indivisible: structural closure, implementation-distinct exact
reconstruction, both NIST CODATA 2022 mass-ratio intervals, custody and adverse
controls must pass in the same engine run. A failed empirical row prevents the
claim entering the model.
