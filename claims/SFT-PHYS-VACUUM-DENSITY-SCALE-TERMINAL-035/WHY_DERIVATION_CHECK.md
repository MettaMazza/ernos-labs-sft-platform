# Terminal vacuum floor, finite radiative ledger and cosmological scale

Claim: `SFT-PHYS-VACUUM-DENSITY-SCALE-TERMINAL-035`

## Why this result matters

The earlier corpus carried several correct but differently typed vacuum facts: a nonempty half-One ground carrier, a finite live-mode spectrum, the global cosmic vacuum share and an asserted `One/2^20` scale. Treating those as one interchangeable number would create the very vacuum-magnitude contradiction the derivation is meant to resolve. This successor law reconstructs each quantity independently, states its support and scale, and rejects any unscaled identification between them.

The result preserves the exact `One/2^20` value, but identifies what the derivation actually forces: the least complete local boundary-energy cell at the generated spatial cover. It separately forces the finite radiative ledger, the global vacuum fraction, the normalized cosmological magnitude and dimensional transport. No continuum zero-point sum, measured dark-energy number, fitted cutoff, gravitational equation or stored V1/V2 survivor enters the forcing runtime.

## Derivation

The independently admitted generator and stable spatial rank are both three. Complete generator-three spatial support therefore has `3^3=27` cells. Binary Fold support progresses `2,4,8,16,32`; depth five is the unique least support that covers twenty-seven.

At each of those five cover levels, a complete local boundary record retains both Fold fibre labels. This forces boundary-record depth `2*5=10`. The least nonempty exact amplitude on that complete record is therefore

`A_floor = One/2^10 = One/1024`.

Energy is not a relabeled one-leg amplitude. A complete preparation-to-record energy observable composes both exact legs, so

`E_floor = A_floor*A_floor = One/2^20 = One/1048576`.

The local support subgrammar explicitly generates all four combinations of one-label/complete-label support and one-leg/two-leg observation. Exactly one candidate retains both labels and both energy legs. The exponent twenty is consequently counted from independently admitted support; it is not selected because the prior corpus named it.

The radiative ledger is separately closed. At positive depth `k`, complete Fold support contains `2^k` half-spacing modes

`(2j-1)/2^(k+1), j=1,...,2^k`.

The first `2^k` odd positive counts sum to `2^(2k)`. The complete mode total is therefore `2^(k-1)`, and division by the complete support leaves common-scale mean `1/2` at every finite depth. The successor doubles the mode count and the total while preserving that half-One mean. No completed infinite sum or continuum density is generated.

The already admitted terminal cosmic budget is a different carrier. Its global vacuum share is `11/16`, with matter `5/16`, and the two reassemble the One. Three generated spatial rate directions then carry the same squared expansion reference, forcing the dimensionless cosmological magnitude

`Lambda(c/H)^2 = 3*(11/16) = 33/16`.

Only after that relation is sealed may held dimensional references be supplied. For an exact positive rate `H` and limiting speed `c`, transport is

`Lambda = (33/16) H^2/c^2`.

Common exact rescaling of `H` and `c` cancels identically, so the transport introduces no second scale or fitted coefficient.

## Exhaustive alternative closure

Twelve binary axes generate `2^12=4,096` outer candidates. They distinguish generated spatial volume from borrowed volume, least binary cover from a selected exponent, complete two-label boundary support from an incomplete record, two-leg energy composition from amplitude relabeling, counted floor from a named prior value, finite complete radiative accounting from an unbounded sum, global `11/16` share from local/global conflation, three-space rate geometry from a borrowed continuum coefficient, typed correction from rubber-stamping, covariant held-reference transport from fitting, post-seal measurement from target access, and no additional rule from a free vacuum scale.

The local boundary/observable construction also has an explicit four-form nested census. Both enumerations have one survivor. The independent validator reconstructs the full 4,096-row order, all decisions, the four-form local census, the finite radiative induction, the exact values and all control classes without importing the claim implementation.

## What is corrected

The older statement `One/2^20` is retained exactly. Its direct relabeling as a global density fraction or dimensionful cosmological constant is rejected because it omits the quantity's support and scale carriers. The withdrawn measurement-influenced expression `2 alpha^4/H^3` is explicitly excluded and does not occur in this claim, its validator or its dependencies.

The four quantities are:

- local boundary-energy floor: `One/2^20`;
- finite radiative common-scale mean: `1/2`;
- global terminal vacuum fraction: `11/16`;
- normalized cosmological magnitude: `33/16`.

Dimensional `Lambda` is the last relation transported through separately held external `H` and `c` references. None of the four dimensionless quantities may be silently substituted for another.

## Machine evidence

- Outer candidate count: `4,096`.
- Nested local candidates: `4`; survivors: `1`.
- Closure: `depth_independent`.
- Source manifest: `sha256:b6466fba1a682b36391e848e061aa30773de6cb511b44b1767e23cb677346772`.
- Derivation seal: `sha256:c01238ae67c8185018e5b6be9ad1cf39d8c2dfc241b7e6e0845381f81fa87586`.
- Independent validation: `sha256:0f107bf8bfbd842816b120cd750e391cd712a567ecd649cad20db2163b818f52`.
- Independent implementation: `sha256:ea8f27dc1760e402a959b6da81c14c9bbc8ecb3c20de37ede2abfc9a0f41fc10`.
- Engine receipt: `sha256:c7b4777b12fc70628b0fd9a2f7d957274b61ede7f3624a665781364c8c9f7723`.

The result is complete against its declared grammar and current evidence while remaining open to lawful extensions admitted through the same untouched engine.
