# Exact entropy and uncertainty from deterministic observation classes

Claim: `SFT-INFO-ENTROPY-UNCERTAINTY-001`

## Why

Entropy and uncertainty must be reconstructed without treating randomness or real logarithms as premises. Exact observation classes already contain the complete unresolved information.

## Derivation

Mathematical probability supplies exact support parts; symbol distinction supplies observation classes; information quantity supplies retained/closed ledgers. Nine axes force an entropy object that preserves every class, part and unavailable distinction.

The registered grammar boundary is:

> All finite uncertainty structures generated from complete deterministic microstate support, total observation partitions, exact class parts, complete within-class pair ledgers and refinement relations.

The complete product has `512` candidates across
`9` declared structural dimensions. Every dimension has one
all-preserving coordinate and one explicit failure coordinate. Exactly one
product member combines all preserving coordinates.

Admitted coordinates:

- `support` — admitted `complete-deterministic-support`: Every registered microstate occurs once.
- `partition` — admitted `total-disjoint-observation-classes`: Every microstate belongs to exactly one observed class.
- `weights` — admitted `exact-class-whole-part`: The class count is retained relative to complete support count.
- `unresolved` — admitted `complete-within-class-pairs`: Every pair of distinct class microstates is retained once.
- `entropy` — admitted `weighted-class-distinction-ledger`: Every class retains exact part, members and unresolved distinctions.
- `refinement` — admitted `class-containment-witness`: Every fine class lies inside one coarse class and its closed-pair set is contained.
- `cause` — admitted `deterministic-support-observation`: Uncertainty comes from distinctions closed by observation over deterministic support.
- `generality` — admitted `microstate-successor`: A fresh state extends exactly one class and its new within-class pairs.
- `addition` — admitted `no-extra-entropy-convention`: The exact ledger follows only from support and observation.

The admitted result is:

> The entropy/uncertainty kernel is complete deterministic support, total observation classes, exact class parts, complete unresolved-pair ledgers, refinement containment, successor closure and no stochastic or log rule.

Its operational laws are:

- observation classes form a disjoint complete partition of deterministic support.
- uncertainty is exactly the set of within-class nonidentity microstate pairs.
- singleton classes carry empty-One uncertainty rather than numerical zero.
- fine observation cannot have more closed pairs than a coarser observation it refines.
- entropy retains class provenance and exact part weights instead of collapsing to a floating scalar.

Depth-independent closure uses this base:

> One deterministic microstate forms one exact whole class with structural empty-One uncertainty.

and this successor certificate:

> Adding one fresh microstate assigns it to one retained observation image, updates that class's exact part and adds exactly one unresolved pair with every prior member of the same class; other classes remain unchanged.

The exact exclusions are:

- no stochastic source premise.
- no logarithmic entropy proof value.
- no numerical zero for singleton uncertainty.
- no fitted probability distribution.

## Check

Execute all 512 entropy kernels, verify fine, prefix and total-coarsening ledgers, exact parts and refinement monotonicity, run adverse controls and independently regenerate the candidate product.

Operational witnesses:

- `fine-certainty`: Fine observation gives every state a singleton class with empty-One uncertainty.
- `prefix-uncertainty`: Prefix observation has two exact half-support classes and one unresolved pair each.
- `coarse-uncertainty`: Complete coarsening retains all six microstate pairs as unresolved.
- `refinement-order`: Fine refines prefix, and prefix refines the one-class observation.
- `closed-pair-monotonicity`: Refinement reduces closed distinctions exactly in the witness chain.

Four required controls reject a false premise, changed source identity, altered
survivor set and excluded boundary import. The separate validator independently
regenerates the literal candidate product and sole survivor without importing the
scientific law module.

## Boundary

This theorem closes exact finite entropy and uncertainty. Conventional scalar Shannon entropies are post-seal summaries only and require a separately declared correspondence map.

Conventional correspondence terms, admitted only after the derivation is sealed:
entropy, uncertainty, partition entropy, resolution, deterministic ensemble.
