# Why this is a derivation check

The velocity ceiling and positive Fold spacing are prior exact structures. Their exact quotient forces the finite gradient bound b^k; no viscosity, damping coefficient or numerical cap is selected. Depth five produces 32 as a consequence, while the depth successor proves the finite bound at every generated depth.
