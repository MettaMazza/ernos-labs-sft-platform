# SFT-PHYS-STRONG-RUNNING-DIRECTION-002: WHY / DERIVATION / CHECK

## WHY

A neutral carrier retains the One source count at every positive level. A colour-carrying mediator appends both Fold labels at every successor level, forcing exact source-count prefix 1,3,5,... with successor difference two. The claim fixes the structural direction and binary slope, not a measured renormalized beta coefficient.

The V1/V2 statement is a registered reconstruction target, never an executable premise or survivor selector. This V3 claim exists so the exact Physics result is independently forced in its categorical branch before any authoritative measurement is opened.

## DERIVATION

The colour self-source count is 1+2k after k successors (prefix 1,3,5,7,...) and the neutral-carrier count remains the One; the exact structural successor slope is two.

Base: The bare source row contains one retained source carrier in both sectors.

Successor: Each colour-feedback successor appends exactly the two Fold labels; the neutral carrier appends none, preserving the prior count.

The complete grammar boundary is: Every positive finite running level generated by retaining the bare source and appending either the complete binary colour feedback pair or no charged feedback.

## CHECK

The engine must enumerate all 256 registered forms, retain exactly one, reject every changed coordinate, pass four hostile controls and receive an implementation-distinct exact reconstruction. Measurement is a later sealed claim and cannot rewrite this formal receipt.
