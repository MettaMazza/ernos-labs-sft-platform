# Post-seal experimental agreement of the inverse-square exponent

Claim: `SFT-PHYS-VALIDATION-INVERSE-SQUARE-001`

## Why

The formal chain forces exponent two before any new target is searched. This
claim asks only whether a registered physical experiment is compatible with
that sealed consequence. It cannot derive, tune or replace the exponent.

## Derivation-to-measurement order

1. `SFT-PHYS-STRUCT-GENERATOR-THREE-001` forces generator three.
2. `SFT-PHYS-SPACE-DIMENSION-THREE-001` forces the unique stable spatial count.
3. `SFT-PHYS-SPACE-BOUNDARY-RANK-TWO-001` forces boundary rank two.
4. `SFT-PHYS-FIELD-INVERSE-SQUARE-001` forces and seals exponent two.
5. Only then is the external Coulomb-law result compared.

Williams, Faller and Hill report `q = (2.7 +/- 3.1) x 10^-16` for a tested
relation `1/r^(2+q)`. The source-bound measurement adapter retains the complete
reported interval without assigning a new confidence meaning. Written as exact
positive exponent bounds, it is:

`199999999999999996/100000000000000000 <= exponent <= 200000000000000058/100000000000000000`.

Positive-successor enumeration places exactly one positive integer in that
interval: two. One and three are retained as explicit boundary controls.

## Check

The claim enumerates all 256 registered comparison forms and leaves one
survivor. An implementation-distinct validator regenerates that product and
the interval census. The external validator verifies the byte hash, DOI,
source identity, complete reported bounds, capability-closed prediction,
post-seal target release and tampered control.

The result is empirical agreement within the published uncertainty interval.
It is not a claim that experiment proves an exact exponent, and no measurement
becomes a dependency of the formal law.
