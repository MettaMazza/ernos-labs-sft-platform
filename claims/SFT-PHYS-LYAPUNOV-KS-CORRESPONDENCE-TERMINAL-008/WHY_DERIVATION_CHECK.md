# SFT-PHYS-LYAPUNOV-KS-CORRESPONDENCE-TERMINAL-008: WHY / DERIVATION / CHECK

## WHY

Expansion of nearby distinctions and growth of distinguishable histories are often represented by separate analytic quantities. The Fold law forces their common exact source: the complete generated label count `m`. This closes V1 PH2 without importing a logarithm, irrational approximation or fitted parameter into the SFT proof domain.

## DERIVATION

Let a Fold have the complete positive label set of cardinality `m`, with `m` at least two. One uncast Fold step copies each local separation across every generated label, so the separation carrier is exactly `m`. A depth-one complete word has `m` possibilities. If complete depth-`d` support has `m^d` words, appending each of the `m` labels to every word produces `m * m^d = m^(d+1)` words. Therefore every support successor also has exact ratio `m`.

The two independently constructed quantities—local-separation growth and complete-support growth—return the same generated positive whole. No additional rate parameter exists. The eight-axis grammar enumerates all 2,916 registered forms; all alternatives importing selected binary support, independent rates, target-assigned values, analytic proof scalars, pre-seal observation or free correction fail. Exactly one form survives.

## CHECK

- The independent implementation rebuilds every candidate identity and decision without importing the claimant or external targets.
- The engine seals the unique survivor, passes all nine gates and records `model_admitted: true`.
- Post-seal release opens two immutable primary-source snapshots and retains every registered comparison row.
- For the directly documented two-branch map, pieces, absolute slope, preimages and output symbols all equal the independently generated carrier two.
- Exact Fold support at depths one through five is `2, 4, 8, 16, 32`; every successor ratio and tested separation multiplier is two.
- The conventional Lyapunov and information-rate expressions are retained as external labels only.
- The entropy-formula comparison retains the stated regularity, invariance and splitting hypotheses; it is not universalized.

## EXCLUSIONS

- No logarithmic or irrational number is an SFT proof value.
- No measurement selects the formal survivor.
- No target value is available to the capability-closed prediction.
- No fitted residual, tolerance or free rate correction is admitted.
- No claim is made that the entropy formula holds outside the external source's stated hypotheses.
