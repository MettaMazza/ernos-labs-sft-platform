# Generation-cover dark-to-baryon fraction

The generated three-by-three-by-three carrier has volume `27`. The least
complete binary support containing it is `2^5 = 32`; the same depth is
cross-locked as binary plus generator, `2 + 3 = 5`. The depth and volume
therefore partition the complete support as `5/32` and `27/32`, forcing the
leading ratio `27/5`.

The complete support is pre-periodic. Its positive predecessor `31` supplies
the exact unit part whose first Fold return has period five. Appending that one
native recurrence part to its own depth forces
`27 / (5 + 1/31) = 279/52`. The up-depth floor, bare tower and opposite
orientation fail their registered structural roles.

All 1,024 forms are generated and one survives. The structural result,
implementation-distinct reconstruction and both complete Planck density rows
with their uncertainties must pass together. External cosmological parameter
estimation is a measurement record, not an SFT premise.
