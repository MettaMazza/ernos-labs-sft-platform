# Why CLASS-007 requires a derivation check

Particle reinforcement retains the applied load partition across particles, matrix and interface together with particle distribution.

The 256-form grammar is generated before target release, independently reconstructed and compared against every registered authoritative record.
