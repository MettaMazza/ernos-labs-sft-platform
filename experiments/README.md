# Empirical evidence

Natural-science experiments preserve development observation separately from
blind test targets. A frozen relation executes without target access, seals its
complete output, and is measured afterward by a separate evaluator.

See `docs/EMPIRICAL_METHOD.md` before registering an experiment.
